---
phase: 01-ftp-endpoint-contract-routing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - core/vfs.go
  - core/vfs_test.go
  - core/vfs_type.go
  - conf/config.go
autonomous: true
requirements:
  - FTP-01
  - FTP-02
  - FTP-03
  - FTP-04
must_haves:
  truths:
    - User can declare an `ftp://` endpoint as source or destination without the path being treated as disk.
    - User can express FTP host, optional port, username, password, remote path, timeout, and passive-mode compatibility in the endpoint contract.
    - Omitting the FTP port still yields a valid endpoint using port `21`.
  artifacts:
    - path: core/vfs.go
      provides: FTP scheme parsing, FTP query fields, and default port behavior.
    - path: core/vfs_test.go
      provides: FTP parse, round-trip, and default-port coverage.
    - path: conf/config.go
      provides: confirmation that `conf.Config` continues to carry `core.VFS` endpoints unchanged.
  key_links:
    - from: core/vfs.go
      to: conf/config.go
      via: Source and Dest fields remain typed as core.VFS.
      pattern: "Source\\s+core\\.VFS|Dest\\s+core\\.VFS"
    - from: core/vfs.go
      to: core/vfs_test.go
      via: FTP endpoint examples and parse assertions.
      pattern: "ftp://"
---

<objective>
Define the FTP endpoint contract inside `core.VFS` so Phase 1 can express FTP endpoints using the repo's existing query-parameter URL grammar.

Purpose: This establishes the exact config surface required by FTP-01 through FTP-04 and removes ambiguity before factory wiring begins.
Output: FTP-aware VFS parsing, getters/storage for FTP-specific endpoint fields, and automated tests proving parse + round-trip behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ftp-endpoint-contract-routing/01-CONTEXT.md
@.planning/phases/01-ftp-endpoint-contract-routing/01-RESEARCH.md
@.planning/phases/01-ftp-endpoint-contract-routing/01-PATTERNS.md
@core/vfs.go
@core/vfs_test.go
@core/vfs_type.go
@conf/config.go

<interfaces>
From `core/vfs.go`:

```go
type VFS struct {
	original          string
	path              Path
	remotePath        Path
	fsType            VFSType
	host              string
	port              int
	server            bool
	fsServer          string
	localSyncDisabled bool
	secure            bool
	sshConf           SSHConfig
}

func NewVFS(path string) VFS
func (vfs *VFS) Type() VFSType
func (vfs *VFS) Host() string
func (vfs *VFS) Port() int
func (vfs *VFS) RemotePath() Path
```

From `conf/config.go`:

```go
type Config struct {
	Source core.VFS `json:"source" yaml:"source"`
	Dest   core.VFS `json:"dest" yaml:"dest"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FTP-specific VFS contract fields and parsing</name>
  <files>core/vfs.go</files>
  <read_first>core/vfs.go, core/vfs_type.go, .planning/phases/01-ftp-endpoint-contract-routing/01-CONTEXT.md, .planning/phases/01-ftp-endpoint-contract-routing/01-RESEARCH.md</read_first>
  <action>Update `core/vfs.go` so `NewVFS` recognizes the `ftp://` scheme per D-01 and D-02. Add FTP-specific query parameter constants and VFS storage/getters for explicit FTP endpoint auth/transport fields per D-03 and D-04. The exposed contract must include FTP-specific username, password, timeout, and passive-mode boolean values plus the existing `remote_path`; do not reuse `ssh_user`, `ssh_pass`, or `SSHConfig`. Add `ftpServerScheme = "ftp"` and default omitted FTP ports to `21` per D-07. Keep timeout optional at endpoint scope per D-08 and model passive compatibility as one boolean only per D-05; do not add additional FTP compatibility flags per D-06. Preserve existing `conf.Config` use of `core.VFS` rather than introducing a new top-level config object.</action>
  <acceptance_criteria>`core/vfs.go` contains an `ftp` scheme branch in `NewVFS`; `core/vfs.go` contains FTP-specific parameter constants rather than `ssh_` names for FTP auth; omitted `ftp://host?...` endpoints resolve port `21` in parser logic; timeout parsing is optional and passive-mode parsing is a single boolean field.</acceptance_criteria>
  <verify>
    <automated>go test ./core -count=1</automated>
  </verify>
  <done>`core.NewVFS("ftp://...")` produces `Type()==FTP` with explicit FTP auth/transport fields available from the parsed VFS, and existing non-FTP scheme behavior remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add FTP VFS round-trip and default-port tests</name>
  <files>core/vfs_test.go, core/vfs_type.go</files>
  <read_first>core/vfs_test.go, core/vfs_type.go, core/vfs.go, .planning/phases/01-ftp-endpoint-contract-routing/01-VALIDATION.md</read_first>
  <action>Extend the existing VFS tests with canonical FTP endpoint strings covering source/destination-style usage, omitted-port behavior, FTP-specific username/password naming, optional timeout, and passive-mode boolean parsing. Follow the existing `testVFS...` constant + table-driven pattern already used for `rs://`, `sftp://`, and `minio://`. Add assertions that FTP round-trips through marshal/unmarshal and flag helpers without degrading to `Disk` or `Unknown`. Keep `core/vfs_type.go` unchanged unless a small test-support export or string coverage adjustment is genuinely required.</action>
  <acceptance_criteria>`core/vfs_test.go` contains at least one canonical `ftp://` endpoint fixture with FTP-specific query keys; test tables for marshal/unmarshal/default-port handling include FTP entries; `go test ./core -count=1` exits 0.</acceptance_criteria>
  <verify>
    <automated>go test ./core -count=1</automated>
  </verify>
  <done>Automated tests prove FTP endpoint parsing, round-trip serialization, and default-port behavior using the same testing style as other remote backends.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config string → `core.NewVFS` | Untrusted endpoint text is parsed into runtime routing state |
| parsed VFS → sync/monitor factories | Parsed FTP state controls which backend path later runtime code enters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `core/vfs.go` parser | mitigate | Parse FTP through explicit scheme and query keys; add tests covering malformed/omitted-port cases so FTP does not silently fall back to a different backend. |
| T-01-02 | I | FTP credential fields in `core/vfs.go` | mitigate | Keep FTP auth fields endpoint-scoped and avoid SSH field reuse so later logging/tests can distinguish FTP secrets from SSH config; do not add password logging in parser changes. |
| T-01-03 | E | backend classification in `core.NewVFS` | mitigate | Assert `ftp://` becomes `core.FTP` in tests so unsupported-path or disk-path fallthrough cannot masquerade as valid routing. |
</threat_model>

<verification>
- Run `go test ./core -count=1`.
- Read `core/vfs_test.go` to confirm FTP fixtures exercise default port, round-trip serialization, and FTP-specific query names.
</verification>

<success_criteria>
- `ftp://` endpoints are parsed as `core.FTP`.
- FTP-specific auth and transport fields are represented explicitly on `core.VFS`.
- Omitted FTP ports default to `21`.
- FTP query contract is covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ftp-endpoint-contract-routing/01-01-SUMMARY.md`
</output>
