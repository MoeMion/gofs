---
phase: 01-ftp-endpoint-contract-routing
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - sync/sync.go
  - monitor/monitor.go
  - sync/ftp_push_client_sync.go
  - sync/ftp_pull_client_sync.go
  - monitor/ftp_pull_client_monitor.go
autonomous: true
requirements:
  - FTP-01
  - FTP-02
must_haves:
  truths:
    - A parsed FTP destination enters an FTP-specific sync factory branch instead of the generic unsupported-path error branch.
    - A parsed FTP source enters an FTP-specific monitor routing branch instead of the generic unsupported-path error branch.
    - FTP routing is thin and isolated, leaving real protocol behavior for Phase 2.
  artifacts:
    - path: sync/sync.go
      provides: FTP source/destination factory routing.
    - path: monitor/monitor.go
      provides: FTP source monitor routing.
    - path: sync/ftp_push_client_sync.go
      provides: FTP-specific sync constructor entry point.
    - path: sync/ftp_pull_client_sync.go
      provides: FTP-specific sync constructor entry point.
    - path: monitor/ftp_pull_client_monitor.go
      provides: FTP-specific pull monitor entry point.
  key_links:
    - from: sync/sync.go
      to: sync/ftp_push_client_sync.go
      via: `source.IsDisk() && dest.Is(core.FTP)` dispatch.
      pattern: "dest\\.Is\\(core\\.FTP\\)"
    - from: sync/sync.go
      to: sync/ftp_pull_client_sync.go
      via: `source.Is(core.FTP) && dest.IsDisk()` dispatch.
      pattern: "source\\.Is\\(core\\.FTP\\)"
    - from: monitor/monitor.go
      to: monitor/ftp_pull_client_monitor.go
      via: source FTP monitor branch.
      pattern: "NewFTPPullClientMonitor"
---

<objective>
Wire FTP endpoints into sync and monitor factory routing using thin FTP-specific entry points that preserve the current architecture boundary.

Purpose: This makes `ftp://` endpoints reachable through the existing orchestration seams now, while keeping real FTP protocol operations in Phase 2.
Output: FTP-specific sync constructors, FTP pull monitor constructor, and factory dispatch branches that no longer treat FTP as a generic unsupported combination.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ftp-endpoint-contract-routing/01-CONTEXT.md
@.planning/phases/01-ftp-endpoint-contract-routing/01-RESEARCH.md
@.planning/phases/01-ftp-endpoint-contract-routing/01-PATTERNS.md
@.planning/phases/01-ftp-endpoint-contract-routing/01-01-PLAN.md
@sync/sync.go
@sync/sftp_push_client_sync.go
@sync/sftp_pull_client_sync.go
@sync/driver_push_client_sync.go
@sync/driver_pull_client_sync.go
@monitor/monitor.go
@monitor/sftp_pull_client_monitor.go

<interfaces>
From `sync/sync.go`:

```go
func NewSync(opt Option) (Sync, error)
```

From `monitor/monitor.go`:

```go
func NewMonitor(opt Option, run runFn) (Monitor, error)
```

From `sync/sftp_push_client_sync.go` and `sync/sftp_pull_client_sync.go`:

```go
func NewSftpPushClientSync(opt Option) (Sync, error)
func NewSftpPullClientSync(opt Option) (Sync, error)
```

From `monitor/sftp_pull_client_monitor.go`:

```go
func NewSftpPullClientMonitor(opt Option) (m Monitor, err error)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add thin FTP sync and monitor entry points</name>
  <files>sync/ftp_push_client_sync.go, sync/ftp_pull_client_sync.go, monitor/ftp_pull_client_monitor.go</files>
  <read_first>sync/sftp_push_client_sync.go, sync/sftp_pull_client_sync.go, monitor/sftp_pull_client_monitor.go, sync/driver_push_client_sync.go, sync/driver_pull_client_sync.go, .planning/phases/01-ftp-endpoint-contract-routing/01-CONTEXT.md</read_first>
  <action>Create FTP-specific constructor files that mirror the thin-wrapper pattern used by SFTP per D-01 through D-04. These entry points must be architecture placeholders for Phase 1, not full protocol implementations: they should establish the FTP-specific constructor surface and return a concrete FTP-specific error that clearly states the FTP driver/backend is deferred to Phase 2 once routing has selected FTP. Do not introduce real protocol behavior, external library usage, or HTTP/server-surface work in this phase. Keep the files narrowly scoped to constructor-level entry points so the factory paths can target FTP explicitly without inventing a new subsystem.</action>
  <acceptance_criteria>`sync/ftp_push_client_sync.go` and `sync/ftp_pull_client_sync.go` exist with exported `NewFTPPushClientSync` and `NewFTPPullClientSync`; `monitor/ftp_pull_client_monitor.go` exists with exported `NewFTPPullClientMonitor`; each file is thin constructor-oriented code and does not import a concrete FTP library.</acceptance_criteria>
  <verify>
    <automated>go test ./sync ./monitor -count=1</automated>
  </verify>
  <done>FTP-specific constructor entry points exist so the factories can route into FTP code paths immediately, while real backend behavior remains owned by Phase 2.</done>
</task>

<task type="auto">
  <name>Task 2: Route FTP through sync and monitor factories with regression tests</name>
  <files>sync/sync.go, monitor/monitor.go</files>
  <read_first>sync/sync.go, monitor/monitor.go, sync/ftp_push_client_sync.go, sync/ftp_pull_client_sync.go, monitor/ftp_pull_client_monitor.go, .planning/phases/01-ftp-endpoint-contract-routing/01-RESEARCH.md</read_first>
  <action>Update `sync.NewSync` so `source.IsDisk() && dest.Is(core.FTP)` dispatches to `NewFTPPushClientSync` and `source.Is(core.FTP) && dest.IsDisk()` dispatches to `NewFTPPullClientSync`. Update `monitor.NewMonitor` so `source.Is(core.FTP)` dispatches to `NewFTPPullClientMonitor`. Preserve existing ordering and non-FTP behavior. Add or extend targeted tests in the affected packages so FTP combinations no longer fall into the generic unsupported-branch logic. The acceptance point for this phase is routing correctness: FTP combinations must enter FTP-specific constructors, even if those constructors intentionally return a Phase-2-deferred backend error.</action>
  <acceptance_criteria>`sync/sync.go` contains FTP push and pull branches; `monitor/monitor.go` contains an FTP source branch; package tests prove FTP source/destination combinations reach FTP-specific constructors rather than the fallback unsupported error path; `go test ./sync ./monitor -count=1` exits 0.</acceptance_criteria>
  <verify>
    <automated>go test ./sync ./monitor -count=1</automated>
  </verify>
  <done>FTP endpoint routing is live in both factories, and automated tests verify that `ftp://` endpoints are routed intentionally instead of being treated as an unknown backend combination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed `core.VFS` → sync factory | Backend classification determines which mutation path runtime code will execute |
| parsed `core.VFS` → monitor factory | Source classification determines which long-running monitor loop is created |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | E | `sync/sync.go` | mitigate | Add explicit FTP branches so `ftp://` combinations cannot accidentally bypass intended routing through the generic unsupported branch. |
| T-01-04 | D | `monitor/monitor.go` | mitigate | Route FTP sources into a defined FTP monitor constructor so long-running FTP source configs fail in a controlled, FTP-specific path rather than at a later ambiguous point. |
| T-01-05 | I | FTP placeholder constructors | mitigate | Return explicit Phase-2-deferred errors without embedding secrets or endpoint credentials in the error text. |
</threat_model>

<verification>
- Run `go test ./sync ./monitor -count=1`.
- Run `go test ./... -count=1` after both plans are complete to ensure routing changes do not regress unrelated packages.
</verification>

<success_criteria>
- `sync.NewSync` has explicit FTP push and pull dispatch branches.
- `monitor.NewMonitor` has an explicit FTP source branch.
- FTP-specific constructors exist as thin routing targets only.
- Automated tests prove FTP endpoints no longer hit the generic unsupported-path fallback for the covered combinations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ftp-endpoint-contract-routing/01-02-SUMMARY.md`
</output>
