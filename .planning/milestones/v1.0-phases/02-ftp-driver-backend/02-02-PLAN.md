---
phase: 02-ftp-driver-backend
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - sync/ftp_push_client_sync.go
  - sync/ftp_pull_client_sync.go
  - sync/sync_test.go
autonomous: true
requirements:
  - FTPD-01
  - FTPD-02
  - FTPD-03
  - FTPD-04
  - FTPD-05
  - FTPD-06
  - FTPD-07
  - FTPD-08
  - FTPD-09
must_haves:
  truths:
    - "disk→FTP and FTP→disk constructors no longer stop at Phase 1 deferred errors"
    - "FTP sync paths use the generic driver-backed sync flows already established for other backends"
    - "FTP pull sync compares remote size and modification time through the new driver behavior"
  artifacts:
    - path: sync/ftp_push_client_sync.go
      provides: driver-backed disk→FTP constructor
    - path: sync/ftp_pull_client_sync.go
      provides: driver-backed FTP→disk constructor
    - path: sync/sync_test.go
      provides: constructor/routing regression coverage for FTP sync paths
  key_links:
    - from: sync/ftp_push_client_sync.go
      to: driver/ftp.NewFTPDriver
      via: sync constructor wiring
      pattern: "NewFTPDriver"
    - from: sync/ftp_pull_client_sync.go
      to: driverPullClientSync
      via: embedded generic pull sync
      pattern: "driverPullClientSync"
---

<objective>
Replace the Phase 1 FTP sync placeholders with real driver-backed constructors so the existing generic driver push/pull sync paths can use the new FTP backend.

Purpose: Complete the integration point between the new driver package and the existing sync architecture without widening scope into a new execution model.
Output: Real FTP push/pull sync constructors and regression tests proving FTP routes into driver-backed behavior instead of deferred placeholder errors.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ftp-driver-backend/02-CONTEXT.md
@.planning/phases/02-ftp-driver-backend/02-RESEARCH.md
@.planning/phases/02-ftp-driver-backend/02-VALIDATION.md
@.planning/phases/01-ftp-endpoint-contract-routing/01-02-SUMMARY.md
@sync/sftp_push_client_sync.go
@sync/sftp_pull_client_sync.go
@sync/minio_push_client_sync.go
@sync/minio_pull_client_sync.go
@sync/driver_push_client_sync.go
@sync/driver_pull_client_sync.go
@sync/sync.go
@core/vfs.go

<interfaces>
From `sync/sync.go`:

```go
func NewFTPPushClientSync(opt Option) (Sync, error)
func NewFTPPullClientSync(opt Option) (Sync, error)
```

From `sync/driver_push_client_sync.go`:

```go
type driverPushClientSync struct {
	diskSync
	basePath string
	driver driver.Driver
}

func newDriverPushClientSync(ds diskSync, basePath string) driverPushClientSync
func (s *driverPushClientSync) start(isSync bool) error
```

From `sync/driver_pull_client_sync.go`:

```go
type driverPullClientSync struct {
	diskSync
	driver driver.Driver
}

func newDriverPullClientSync(ds diskSync) driverPullClientSync
func (s *driverPullClientSync) start() error
```

From prior analogs:

```go
// sftp push
s.driver = sftp.NewSFTPDriver(s.remoteAddr, dest.SSHConfig(), true, r, maxTranRate, logger)

// sftp pull resets
s.diskSync.sourceAbsPath = source.RemotePath().Base()
s.diskSync.isDirFn = s.IsDir
s.diskSync.statFn = s.driver.Stat
s.diskSync.getFileTimeFn = s.driver.GetFileTime
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace FTP deferred constructors with real driver-backed sync constructors</name>
  <read_first>
    - sync/ftp_push_client_sync.go
    - sync/ftp_pull_client_sync.go
    - sync/sftp_push_client_sync.go
    - sync/sftp_pull_client_sync.go
    - sync/minio_push_client_sync.go
    - sync/minio_pull_client_sync.go
    - sync/driver_push_client_sync.go
    - sync/driver_pull_client_sync.go
    - driver/ftp/ftp.go
    - core/vfs.go
    - .planning/phases/02-ftp-driver-backend/02-CONTEXT.md
  </read_first>
  <files>sync/ftp_push_client_sync.go, sync/ftp_pull_client_sync.go</files>
  <acceptance_criteria>
    - `sync/ftp_push_client_sync.go` contains `newDriverPushClientSync`
    - `sync/ftp_push_client_sync.go` contains `ftp.NewFTPDriver`
    - `sync/ftp_pull_client_sync.go` contains `newDriverPullClientSync`
    - `sync/ftp_pull_client_sync.go` contains `ftp.NewFTPDriver`
    - `sync/ftp_pull_client_sync.go` contains `s.diskSync.sourceAbsPath = source.RemotePath().Base()`
    - `sync/ftp_pull_client_sync.go` contains `s.diskSync.getFileTimeFn = s.driver.GetFileTime`
  </acceptance_criteria>
  <action>Per D-01, D-02, and D-11, replace the Phase 1 placeholder errors in `sync/ftp_push_client_sync.go` and `sync/ftp_pull_client_sync.go` with the same driver-backed constructor shape already used by SFTP and MinIO. Import the new `driver/ftp` package. In `NewFTPPushClientSync`, read `dest`, `chunkSize`, `maxTranRate`, `Retry`, `Logger`, `SyncOnce`, and `SyncCron`; validate `chunkSize > 0`; create the base disk sync with `newDiskSync(opt)`; embed `newDriverPushClientSync(*ds, dest.RemotePath().Base())`; set `remoteAddr := dest.Addr()`; instantiate `ftp.NewFTPDriver(remoteAddr, dest.FTPConfig(), true, r, maxTranRate, logger)`; then call `s.start(isSync)` exactly like the SFTP push path. In `NewFTPPullClientSync`, mirror the SFTP pull constructor: create `driverPullClientSync`, instantiate `ftp.NewFTPDriver(source.Addr(), source.FTPConfig(), true, r, maxTranRate, logger)`, call `s.start()`, then set `s.diskSync.sourceAbsPath = source.RemotePath().Base()`, `s.diskSync.isDirFn = s.IsDir`, `s.diskSync.statFn = s.driver.Stat`, and `s.diskSync.getFileTimeFn = s.driver.GetFileTime` so the generic pull path compares FTP remote size/time through the new driver. Do not add FTP-specific sync semantics beyond constructor wiring; reuse the existing `driverPushClientSync` and `driverPullClientSync` flows exactly as intended by D-02.</action>
  <verify>
    <automated>go test ./sync -count=1</automated>
  </verify>
  <done>The FTP push/pull constructors create real driver-backed syncers, reuse the existing generic driver sync flows, and stop returning the Phase 1 deferred backend error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update sync regression tests to prove FTP routes into the real backend path</name>
  <read_first>
    - sync/sync_test.go
    - sync/ftp_push_client_sync.go
    - sync/ftp_pull_client_sync.go
    - .planning/phases/01-ftp-endpoint-contract-routing/01-02-SUMMARY.md
    - .planning/codebase/TESTING.md
  </read_first>
  <files>sync/sync_test.go</files>
  <behavior>
    - Test 1: disk→FTP no longer returns `errFTPBackendDeferred`
    - Test 2: FTP→disk no longer returns `errFTPBackendDeferred`
    - Test 3: unsupported non-FTP combinations still return `errFileSystemUnsupported`
    - Test 4: constructor tests can assert FTP paths instantiate driver-backed sync types without needing a live server connection
  </behavior>
  <acceptance_criteria>
    - `sync/sync_test.go` does not contain `expected ftp deferred error`
    - `sync/sync_test.go` contains assertions for non-deferred FTP routing behavior
    - `go test ./sync -count=1` exits 0
    - `go test ./driver/ftp ./sync -count=1` exits 0
  </acceptance_criteria>
  <action>Rewrite `sync/sync_test.go` so it validates the new Phase 2 contract instead of the old placeholder behavior from Phase 1. Replace the deferred-error expectations with tests that prove FTP combinations route into the new constructor path and that non-FTP unsupported combinations still fall back to `errFileSystemUnsupported`. If direct constructor execution would immediately try to connect to a real FTP endpoint, add a narrow package-level test seam in the FTP sync constructors for the driver factory and override it in tests so the assertions can confirm constructor type/wiring deterministically without network access. Keep the seam unexported and limited to constructor testing; production behavior must still use `ftp.NewFTPDriver`. Preserve the focused regression-test style introduced in Phase 1 instead of broad end-to-end testing here.</action>
  <verify>
    <automated>go test ./driver/ftp ./sync -count=1</automated>
  </verify>
  <done>Sync package tests prove FTP routing now reaches real driver-backed constructors, the old deferred backend error is gone from the FTP path, and unsupported non-FTP combinations still behave as before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VFS FTP config → sync constructors | Endpoint credentials, timeout, and remote paths flow from parsed config into runtime constructor wiring |
| sync generic layer → FTP driver | The generic driver sync paths assume the FTP driver reports metadata and errors honestly |
| test seam → production constructor path | Narrow test-only seams must not alter production FTP driver selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | T | `sync/ftp_*_client_sync.go` constructor wiring | mitigate | Instantiate `ftp.NewFTPDriver` directly in production code and keep any test seam unexported so runtime behavior cannot be swapped accidentally |
| T-02-07 | I | pull sync metadata comparison | mitigate | Wire `statFn` and `getFileTimeFn` to the FTP driver so pull comparisons use the conservative driver metadata policy rather than ad hoc defaults |
| T-02-08 | D | constructor startup | mitigate | Preserve existing `chunkSize` validation and reuse `start()` methods so constructor failures surface early with explicit errors |
| T-02-09 | R | regression coverage | mitigate | Replace Phase 1 deferred-error assertions with tests that prove the real backend path is selected and unsupported non-FTP routing still fails explicitly |
</threat_model>

<verification>
- `go test ./sync -count=1`
- `go test ./driver/ftp ./sync -count=1`
- Confirm `sync/ftp_push_client_sync.go` and `sync/ftp_pull_client_sync.go` no longer return `errFTPBackendDeferred`.
</verification>

<success_criteria>
- FTP sync constructors are real driver-backed constructors, not placeholders.
- FTP routing tests validate the new backend path and preserve unsupported fallbacks for unrelated combinations.
- Pull sync metadata comparison is explicitly wired to the FTP driver's `Stat` and `GetFileTime` behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ftp-driver-backend/02-02-SUMMARY.md`
</output>
