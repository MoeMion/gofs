---
phase: 03-one-way-ftp-sync-flows
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - sync/sync_test.go
  - sync/ftp_push_client_sync.go
  - sync/ftp_pull_client_sync.go
  - sync/driver_push_client_sync.go
  - sync/driver_pull_client_sync.go
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-04
must_haves:
  truths:
    - "User can run disk→FTP using the existing one-way driver push semantics"
    - "User can run FTP→disk using the existing one-way driver pull semantics"
    - "FTP-backed flows preserve delete and rename handling where the backend supports them"
    - "A second sync run can no-op when FTP metadata is sufficiently supported, while ambiguous metadata still prefers safety"
  artifacts:
    - path: sync/sync_test.go
      provides: targeted FTP flow semantics and no-op regression coverage
    - path: sync/driver_push_client_sync.go
      provides: existing disk→FTP one-way mutation behavior used by FTP
    - path: sync/driver_pull_client_sync.go
      provides: existing FTP→disk one-way comparison and write behavior used by FTP
  key_links:
    - from: sync/ftp_push_client_sync.go
      to: sync/driver_push_client_sync.go
      via: embedded driverPushClientSync
      pattern: "newDriverPushClientSync"
    - from: sync/ftp_pull_client_sync.go
      to: sync/driver_pull_client_sync.go
      via: embedded driverPullClientSync
      pattern: "newDriverPullClientSync"
    - from: sync/driver_pull_client_sync.go
      to: driver.Driver.GetFileTime
      via: FTP pull metadata comparison
      pattern: "getFileTimeFn = s.driver.GetFileTime|QuickCompare"
---

<objective>
Lock down the real one-way FTP flow semantics — create, update, delete, rename, and conservative no-op behavior — using the existing generic sync architecture per D-01, D-02, D-05, D-06, D-07, and D-08.

Purpose: Once the FTP source monitor exists, Phase 3 still needs proof that both `disk→FTP` and `FTP→disk` are operationally usable without changing gofs semantics or hiding FTP metadata caveats.
Output: Targeted FTP flow tests, plus only the minimal sync-layer fixes required if those tests reveal a real semantic gap.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-one-way-ftp-sync-flows/03-CONTEXT.md
@.planning/phases/03-one-way-ftp-sync-flows/03-RESEARCH.md
@.planning/phases/03-one-way-ftp-sync-flows/03-VALIDATION.md
@.planning/phases/02-ftp-driver-backend/02-01-SUMMARY.md
@.planning/phases/02-ftp-driver-backend/02-02-SUMMARY.md
@sync/ftp_push_client_sync.go
@sync/ftp_pull_client_sync.go
@sync/driver_push_client_sync.go
@sync/driver_pull_client_sync.go
@sync/sync_test.go
@sync/disk_sync.go
@driver/driver.go

<interfaces>
From `sync/driver_push_client_sync.go`:

```go
func (s *driverPushClientSync) Create(path string) error
func (s *driverPushClientSync) Write(path string) error
func (s *driverPushClientSync) Remove(path string) error
func (s *driverPushClientSync) Rename(path string) error
func (s *driverPushClientSync) SyncOnce(path string) error
```

From `sync/driver_pull_client_sync.go`:

```go
func (s *driverPullClientSync) Write(path string) error
func (s *driverPullClientSync) Remove(path string) error
func (s *driverPullClientSync) Rename(path string) error
func (s *driverPullClientSync) SyncOnce(path string) error
```

From `sync/disk_sync.go`:

```go
func (s *diskSync) Remove(path string) error
func (s *diskSync) Rename(path string) error
func (s *diskSync) syncWalk(currentPath string, d fs.DirEntry, sync Sync, readLink func(path string) (string, error)) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add targeted FTP flow tests for one-way semantics and conservative no-op behavior</name>
  <read_first>
    - sync/sync_test.go
    - sync/ftp_push_client_sync.go
    - sync/ftp_pull_client_sync.go
    - sync/driver_push_client_sync.go
    - sync/driver_pull_client_sync.go
    - sync/disk_sync.go
    - .planning/phases/03-one-way-ftp-sync-flows/03-CONTEXT.md
    - .planning/phases/03-one-way-ftp-sync-flows/03-RESEARCH.md
    - .planning/codebase/TESTING.md
  </read_first>
  <files>sync/sync_test.go</files>
  <behavior>
    - Test 1: `disk→FTP` routing still uses `*ftpPushClientSync` and preserves delete/rename-capable driver push behavior
    - Test 2: `FTP→disk` routing still uses `*ftpPullClientSync` and preserves delete/remove behavior through generic pull semantics
    - Test 3: second-run no-op behavior is allowed only when size plus supported modification-time metadata match
    - Test 4: ambiguous FTP metadata must not produce a false no-op decision that hides a real change
  </behavior>
  <acceptance_criteria>
    - `sync/sync_test.go` contains FTP tests covering push semantics, pull semantics, and no-op comparison behavior
    - `sync/sync_test.go` contains a test that asserts ambiguous metadata does not cause a false skip
    - `go test ./sync -count=1` exits 0
  </acceptance_criteria>
  <action>Add focused FTP semantics coverage to `sync/sync_test.go` using the repository's table-driven style and narrow fakes, not a live FTP server. Keep this in Phase 3 package tests rather than Phase 4 integration fixtures. Extend or replace the current fake FTP driver so tests can model file size, mod time, `GetFileTime`, delete, and rename behavior deterministically. Cover SYNC-01 and SYNC-02 by asserting FTP routes still produce `*ftpPushClientSync` and `*ftpPullClientSync`. Cover SYNC-03 by asserting the underlying driver-backed flow still exposes delete/remove and rename-capable paths rather than FTP-specific semantic divergence. Cover SYNC-04 by adding comparison tests around the FTP pull and push no-op decisions: when size and supported modification time are equal, the flow should skip unnecessary transfer; when mod-time fidelity is missing, coarse, or inconsistent, the flow must not falsely report a no-op. Use exact expectations from D-07 and D-08: safe extra transfer is acceptable, missed change is not.</action>
  <verify>
    <automated>go test ./sync -count=1</automated>
  </verify>
  <done>FTP flow tests describe and prove the intended one-way semantics and conservative no-op policy for both directions.</done>
</task>

<task type="auto">
  <name>Task 2: Apply only the minimal sync-layer fixes required by the new FTP flow tests</name>
  <read_first>
    - sync/sync_test.go
    - sync/ftp_push_client_sync.go
    - sync/ftp_pull_client_sync.go
    - sync/driver_push_client_sync.go
    - sync/driver_pull_client_sync.go
    - sync/disk_sync.go
  </read_first>
  <files>sync/ftp_push_client_sync.go, sync/ftp_pull_client_sync.go, sync/driver_push_client_sync.go, sync/driver_pull_client_sync.go</files>
  <acceptance_criteria>
    - `go test ./sync -count=1` exits 0
    - `go test ./driver/ftp ./monitor ./sync -count=1` exits 0
    - no new FTP-specific orchestration type is introduced outside the existing constructor and generic driver sync paths
    - any changed file still routes through `driverPushClientSync` or `driverPullClientSync`
  </acceptance_criteria>
  <action>Run the new tests first and then make only the minimal code changes they prove necessary. Honor D-02 by keeping FTP-specific logic thin: do not introduce a new FTP-only sync engine, conflict resolver, or special mutation model. If the tests reveal a real gap, fix it inside the existing constructor or generic driver-backed sync seams. Examples of acceptable changes: ensuring FTP constructor wiring preserves metadata hooks, tightening a no-op comparison path so ambiguous FTP metadata does not create a false skip, or preserving delete/rename propagation through the existing driver-backed flow. Examples of prohibited changes: adding bidirectional conflict handling, silently swallowing FTP capability errors, or broadening scope into Phase 4 integration/doc work. After fixes, keep verification centered on `./sync`, `./monitor`, and `./driver/ftp` package commands.</action>
  <verify>
    <automated>go test ./driver/ftp ./monitor ./sync -count=1</automated>
  </verify>
  <done>Both FTP directions have passing targeted semantics coverage, any proven sync-layer gap is fixed with minimal architecture-preserving changes, and no-op handling remains conservative rather than optimistic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FTP metadata → sync comparison logic | Remote size/time data may be coarse or ambiguous and influences no-op transfer decisions |
| generic sync helpers → FTP backend | One-way delete/rename/write semantics depend on truthful driver capabilities and comparison wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | I | `sync/driver_pull_client_sync.go` / `sync/driver_push_client_sync.go` comparison logic | mitigate | Add tests that prove ambiguous FTP metadata cannot trigger a false no-op skip; preserve conservative transfer bias when fidelity is uncertain |
| T-03-05 | T | FTP flow semantics | mitigate | Keep FTP on the existing generic driver-backed sync paths and add regression tests for delete/rename/remove behavior so protocol-specific drift is caught quickly |
| T-03-06 | R | capability failure handling | mitigate | Do not add soft-warning fallbacks; preserve explicit errors from the FTP driver so unsupported operations remain visible to callers and logs |
</threat_model>

<verification>
- `go test ./sync -count=1`
- `go test ./driver/ftp ./monitor ./sync -count=1`
- Confirm no changed file introduces a new FTP-specific sync orchestration type outside existing constructor wiring and generic driver sync helpers.
</verification>

<success_criteria>
- `disk→FTP` and `FTP→disk` are both covered by targeted automated flow tests.
- Existing one-way delete/rename/no-conflict semantics are preserved for FTP-backed flows.
- Supported-metadata second-run no-op behavior is verified without risking missed changes when metadata is ambiguous.
</success_criteria>

<output>
After completion, create `.planning/phases/03-one-way-ftp-sync-flows/03-02-SUMMARY.md`
</output>
