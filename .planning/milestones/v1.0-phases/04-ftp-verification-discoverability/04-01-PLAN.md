---
phase: 04-ftp-verification-discoverability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ftp/init-ftp.sh
  - scripts/ftp/server.py
  - integration/testdata/conf/run-gofs-ftp-push-client.yaml
  - integration/testdata/conf/run-gofs-ftp-pull-client.yaml
  - integration/testdata/test/test-gofs-ftp-push.yaml
  - integration/testdata/test/test-gofs-ftp-pull.yaml
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "Maintainers can start a real plain-FTP test server from repository-owned scripts"
    - "The integration harness has reusable FTP push and pull fixture configs"
    - "FTP test scenarios cover nested paths plus delete or rename-relevant behavior through real protocol operations"
  artifacts:
    - path: scripts/ftp/init-ftp.sh
      provides: repo-owned FTP test server bootstrap
    - path: scripts/ftp/server.py
      provides: deterministic local plain-FTP server implementation for tests
    - path: integration/testdata/conf/run-gofs-ftp-push-client.yaml
      provides: FTP push runtime fixture
    - path: integration/testdata/conf/run-gofs-ftp-pull-client.yaml
      provides: FTP pull runtime fixture
    - path: integration/testdata/test/test-gofs-ftp-push.yaml
      provides: push assertions for nested paths and deletion behavior
    - path: integration/testdata/test/test-gofs-ftp-pull.yaml
      provides: pull assertions for nested paths and deletion behavior
  key_links:
    - from: scripts/ftp/init-ftp.sh
      to: scripts/ftp/server.py
      via: background server startup
      pattern: "python3 .*server.py"
    - from: integration/testdata/conf/run-gofs-ftp-push-client.yaml
      to: integration/testdata/test/test-gofs-ftp-push.yaml
      via: TestIntegration_FTP push case
      pattern: "run-gofs-ftp-push-client|test-gofs-ftp-push"
    - from: integration/testdata/conf/run-gofs-ftp-pull-client.yaml
      to: integration/testdata/test/test-gofs-ftp-pull.yaml
      via: TestIntegration_FTP pull case
      pattern: "run-gofs-ftp-pull-client|test-gofs-ftp-pull"
---

<objective>
Create the real FTP integration-test foundation so Phase 4 verifies the shipped backend with repository-owned setup instead of ad hoc local services, per D-01 through D-06.

Purpose: Without deterministic bootstrap scripts and FTP-specific YAML fixtures, the later tagged test suite would still depend on manual environment prep and would not satisfy the phase’s realistic verification goal.
Output: FTP bootstrap scripts plus integration config/test fixtures for both `disk→FTP` and `FTP→disk`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ftp-verification-discoverability/04-CONTEXT.md
@.planning/phases/04-ftp-verification-discoverability/04-RESEARCH.md
@.planning/phases/04-ftp-verification-discoverability/04-VALIDATION.md
@.planning/codebase/TESTING.md
@integration/integration_test.go
@integration/integration_sftp_test.go
@integration/integration_minio_test.go
@integration/testdata/conf/run-gofs-sftp-push-client.yaml
@integration/testdata/conf/run-gofs-sftp-pull-client.yaml
@integration/testdata/test/test-gofs-sftp-push.yaml
@integration/testdata/test/test-gofs-sftp-pull.yaml
@scripts/sftp/init-sftp.sh
@scripts/minio/install-minio.sh

<interfaces>
From `integration/integration_test.go`:

```go
func testIntegrationClientServer(t *testing.T, runServerConf string, runClientConf string, testConf string)
func getRunConf(conf string) string
func getTestConf(conf string) string
```

From `integration/integration_sftp_test.go` / `integration/integration_minio_test.go`:

```go
testCases := []struct {
	name          string
	runServerConf string
	runClientConf string
	testConf      string
}{}
```

These fixtures must remain compatible with the existing `integration` package pattern; only the config/test YAML names should change for FTP.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repository-owned FTP integration bootstrap scripts</name>
  <read_first>
    - scripts/sftp/init-sftp.sh
    - scripts/minio/install-minio.sh
    - .planning/phases/04-ftp-verification-discoverability/04-CONTEXT.md
    - .planning/phases/04-ftp-verification-discoverability/04-RESEARCH.md
  </read_first>
  <files>scripts/ftp/init-ftp.sh, scripts/ftp/server.py</files>
  <acceptance_criteria>
    - `scripts/ftp/init-ftp.sh` contains `pyftpdlib`
    - `scripts/ftp/init-ftp.sh` contains `127.0.0.1`
    - `scripts/ftp/init-ftp.sh` contains `2121`
    - `scripts/ftp/init-ftp.sh` writes a PID or log file so startup failure is inspectable
    - `scripts/ftp/server.py` creates a writable FTP user with read/write/delete/rename permissions
    - `scripts/ftp/server.py` configures passive ports explicitly
  </acceptance_criteria>
  <action>Create `scripts/ftp/init-ftp.sh` and `scripts/ftp/server.py` to provision a local plain-FTP server for integration tests per D-01, D-02, D-04, D-05, and D-06. Use Python 3 plus `pyftpdlib` instead of a manual system daemon so the setup stays repository-owned and repeatable. Bind the server to `127.0.0.1:2121`, create a dedicated FTP workspace directory under the repo/integration workspace, and provision a test user such as `ftp_user` / `ftp_pwd` with full read/write/delete/rename permissions so Phase 4 can verify both directions and delete/rename-relevant behavior. Configure an explicit passive port range in `server.py` so CI data connections are deterministic. Keep the implementation plain FTP only per D-11 — no TLS/FTPS paths, no active mode, no extra compatibility toggles. Make `init-ftp.sh` install the lightweight runtime dependency, create the workspace, start the Python helper in the background, and write a pid/log artifact that later test and CI steps can inspect.</action>
  <verify>
    <automated>bash scripts/ftp/init-ftp.sh</automated>
  </verify>
  <done>The repository can start a deterministic local FTP test server through scripts alone, with no manual host preparation beyond running the script.</done>
</task>

<task type="auto">
  <name>Task 2: Add FTP integration runtime configs and scenario fixtures</name>
  <read_first>
    - integration/testdata/conf/run-gofs-sftp-push-client.yaml
    - integration/testdata/conf/run-gofs-sftp-pull-client.yaml
    - integration/testdata/test/test-gofs-sftp-push.yaml
    - integration/testdata/test/test-gofs-sftp-pull.yaml
    - .planning/phases/03-one-way-ftp-sync-flows/03-02-SUMMARY.md
    - .planning/phases/04-ftp-verification-discoverability/04-CONTEXT.md
  </read_first>
  <files>integration/testdata/conf/run-gofs-ftp-push-client.yaml, integration/testdata/conf/run-gofs-ftp-pull-client.yaml, integration/testdata/test/test-gofs-ftp-push.yaml, integration/testdata/test/test-gofs-ftp-pull.yaml</files>
  <acceptance_criteria>
    - `integration/testdata/conf/run-gofs-ftp-push-client.yaml` contains `ftp://127.0.0.1:2121`
    - `integration/testdata/conf/run-gofs-ftp-push-client.yaml` contains `ftp_user=ftp_user`
    - `integration/testdata/conf/run-gofs-ftp-push-client.yaml` contains `ftp_passive=true`
    - `integration/testdata/conf/run-gofs-ftp-pull-client.yaml` contains `sync_cron:` or `sync_once:` to match supported FTP source startup semantics from Phase 3
    - `integration/testdata/test/test-gofs-ftp-push.yaml` contains nested path assertions and at least one delete or rename-relevant assertion
    - `integration/testdata/test/test-gofs-ftp-pull.yaml` contains nested path assertions and at least one delete or rename-relevant assertion
  </acceptance_criteria>
  <action>Add FTP-specific integration fixtures by mirroring the existing SFTP/MinIO shape per D-02 and D-03, while using the actual FTP endpoint grammar shipped in Phases 1-3. Create `run-gofs-ftp-push-client.yaml` for `disk→FTP` and `run-gofs-ftp-pull-client.yaml` for `FTP→disk`; both must target `ftp://127.0.0.1:2121` and include `ftp_user=ftp_user`, `ftp_pass=ftp_pwd`, `ftp_passive=true`, and the real `ftp_*` query parameters from Phase 1. For the pull config, include `sync_cron: "*/5 * * * * *"` or another valid supported mode so the fixture honors the Phase 3 startup truthfulness rule. Add `test-gofs-ftp-push.yaml` and `test-gofs-ftp-pull.yaml` using the same fsctl action language as SFTP/MinIO, but make the assertions specifically cover TEST-01, TEST-02, and TEST-03: nested directories/files must be created and verified, content updates must be checked, and at least one delete or rename-relevant flow must be asserted on each direction. Do not add FTPS or FTP↔FTP fixtures, and do not rely on symlink expectations because the FTP driver truthfully reports unsupported symlink behavior from Phase 2.</action>
  <verify>
    <automated>go test ./integration -tags=integration_test_ftp -run TestIntegration_FTP -count=1</automated>
  </verify>
  <done>FTP has push and pull config/test fixtures that slot into the existing integration harness and prove realistic nested-path plus delete/rename-relevant scenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/test runner → local FTP service | Untrusted process startup must produce the intended plain-FTP passive-only test environment |
| fixture definitions → real FTP mutations | YAML actions can create, remove, or overwrite files on the FTP workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `scripts/ftp/init-ftp.sh` / `scripts/ftp/server.py` | mitigate | Bind only to `127.0.0.1`, use dedicated test credentials/workspace, and keep setup plain FTP passive-only per D-11 rather than broadening the exposed surface |
| T-04-02 | R | FTP integration YAML fixtures | mitigate | Encode exact nested-path and delete/rename-relevant assertions in the fixture files so real protocol regressions are observable rather than inferred |
| T-04-03 | D | FTP data connection setup | mitigate | Configure explicit passive ports in the FTP helper and use a repo-owned startup script so CI flakiness from random data ports is reduced |
</threat_model>

<verification>
- `bash scripts/ftp/init-ftp.sh`
- `go test ./integration -tags=integration_test_ftp -run TestIntegration_FTP -count=1`
- Confirm the FTP fixtures use `ftp://127.0.0.1:2121` and `ftp_*` parameters instead of SFTP/MinIO fields.
</verification>

<success_criteria>
- The repository can provision its own FTP test environment through scripts.
- FTP push and pull fixtures exist in the established integration/testdata layout.
- The fixtures exercise real nested-path plus delete/rename-relevant FTP flows without widening protocol scope.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ftp-verification-discoverability/04-01-SUMMARY.md`
</output>
