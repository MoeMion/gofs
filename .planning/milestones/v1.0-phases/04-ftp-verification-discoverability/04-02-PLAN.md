---
phase: 04-ftp-verification-discoverability
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - integration/integration_ftp_test.go
  - .github/workflows/go.yml
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "Automated tests run real `disk→FTP` and `FTP→disk` flows against an FTP server"
    - "Tagged FTP integration coverage is repeatable in CI, not only on one developer machine"
    - "Nested path and delete or rename behavior are enforced by the real-server suite"
  artifacts:
    - path: integration/integration_ftp_test.go
      provides: tagged FTP integration test entrypoint
    - path: .github/workflows/go.yml
      provides: CI execution of FTP integration coverage
  key_links:
    - from: integration/integration_ftp_test.go
      to: integration/testdata/conf/run-gofs-ftp-push-client.yaml
      via: push test case
      pattern: "run-gofs-ftp-push-client.yaml"
    - from: integration/integration_ftp_test.go
      to: integration/testdata/conf/run-gofs-ftp-pull-client.yaml
      via: pull test case
      pattern: "run-gofs-ftp-pull-client.yaml"
    - from: .github/workflows/go.yml
      to: scripts/ftp/init-ftp.sh
      via: Ubuntu setup step
      pattern: "init-ftp.sh"
---

<objective>
Turn the FTP fixtures into enforced automated verification by adding a tagged real-server suite and CI wiring, satisfying the core trust goal of Phase 4 per D-01 through D-06.

Purpose: Fixtures alone do not prove anything until the integration package and CI both execute them consistently.
Output: `integration/integration_ftp_test.go` plus GitHub Actions steps that provision FTP and run the tagged suite.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ftp-verification-discoverability/04-CONTEXT.md
@.planning/phases/04-ftp-verification-discoverability/04-RESEARCH.md
@.planning/phases/04-ftp-verification-discoverability/04-VALIDATION.md
@.planning/phases/04-ftp-verification-discoverability/04-01-PLAN.md
@integration/integration_test.go
@integration/integration_sftp_test.go
@integration/integration_minio_test.go
@.github/workflows/go.yml

<interfaces>
From `integration/integration_test.go`:

```go
func testIntegrationClientServer(t *testing.T, runServerConf string, runClientConf string, testConf string)
```

From `.github/workflows/go.yml`:

```yaml
- name: Test Integration MinIO
  run: go test -v -race -tags=integration_test_minio ./integration

- name: Test Integration SFTP
  run: go test -v -race -tags=integration_test_sftp ./integration
```

The FTP additions should mirror this exact “backend init step + tagged integration command” workflow shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a tagged FTP integration suite that executes the real fixture pairs</name>
  <read_first>
    - integration/integration_test.go
    - integration/integration_sftp_test.go
    - integration/integration_minio_test.go
    - integration/testdata/conf/run-gofs-ftp-push-client.yaml
    - integration/testdata/conf/run-gofs-ftp-pull-client.yaml
    - integration/testdata/test/test-gofs-ftp-push.yaml
    - integration/testdata/test/test-gofs-ftp-pull.yaml
  </read_first>
  <files>integration/integration_ftp_test.go</files>
  <acceptance_criteria>
    - `integration/integration_ftp_test.go` starts with `//go:build integration_test_ftp`
    - `integration/integration_ftp_test.go` contains `func TestIntegration_FTP(t *testing.T)`
    - `integration/integration_ftp_test.go` contains both `run-gofs-ftp-push-client.yaml` and `run-gofs-ftp-pull-client.yaml`
    - `integration/integration_ftp_test.go` contains both `test-gofs-ftp-push.yaml` and `test-gofs-ftp-pull.yaml`
  </acceptance_criteria>
  <action>Create `integration/integration_ftp_test.go` using the same thin tagged-test structure as SFTP and MinIO per D-01, D-02, and D-03. Add `//go:build integration_test_ftp`, declare `TestIntegration_FTP`, and enumerate at least two cases: one for `disk→FTP` using `run-gofs-ftp-push-client.yaml` + `test-gofs-ftp-push.yaml`, and one for `FTP→disk` using `run-gofs-ftp-pull-client.yaml` + `test-gofs-ftp-pull.yaml`. Keep the body thin by calling `testIntegrationClientServer(...)`; do not invent a custom runner or a second integration framework. Make the test names explicitly mention FTP push/pull so failures are obvious in CI output.</action>
  <verify>
    <automated>go test -v -race -tags=integration_test_ftp ./integration -run TestIntegration_FTP -count=1</automated>
  </verify>
  <done>The integration package contains a real FTP suite that exercises the Phase 4 fixture pairs through the standard process-level test harness.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FTP provisioning and tagged integration execution into CI</name>
  <read_first>
    - .github/workflows/go.yml
    - scripts/ftp/init-ftp.sh
    - integration/integration_ftp_test.go
    - .planning/phases/04-ftp-verification-discoverability/04-RESEARCH.md
  </read_first>
  <files>.github/workflows/go.yml</files>
  <acceptance_criteria>
    - `.github/workflows/go.yml` contains a step named `Init FTP`
    - `.github/workflows/go.yml` contains `./scripts/ftp/init-ftp.sh`
    - `.github/workflows/go.yml` contains a step named `Test Integration FTP`
    - `.github/workflows/go.yml` contains `-tags=integration_test_ftp ./integration`
    - both FTP steps are gated to Ubuntu in the same style as SFTP/MinIO backend setup
  </acceptance_criteria>
  <action>Update `.github/workflows/go.yml` to provision and run FTP integration coverage in the existing Linux-only backend section per D-04, D-05, and D-06. Add an Ubuntu-gated `Init FTP` step that marks `scripts/ftp/init-ftp.sh` executable and runs it. Add an Ubuntu-gated `Test Integration FTP` step that runs `go test -v -race -tags=integration_test_ftp ./integration`. Place these steps alongside the existing SFTP and MinIO backend steps so FTP becomes a first-class protocol verification path rather than a one-off local-only test. Do not broaden CI to Windows/macOS for FTP setup in this phase; follow the existing backend-specific Ubuntu-only pattern.</action>
  <verify>
    <automated>go test -v -race -tags=integration_test_ftp ./integration -run TestIntegration_FTP -count=1</automated>
  </verify>
  <done>CI now provisions the FTP test service and executes the tagged real-server FTP suite in the same workflow family as SFTP and MinIO.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration suite → real FTP service | Test code drives real authentication and filesystem mutations across the network boundary |
| CI workflow → backend setup scripts | Workflow correctness determines whether FTP verification is actually enforced or silently skipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | R | `integration/integration_ftp_test.go` | mitigate | Use explicit tagged cases for push and pull fixture pairs so TEST-01/02/03 failures surface as named CI failures instead of being hidden in generic package tests |
| T-04-05 | T | `.github/workflows/go.yml` FTP steps | mitigate | Add dedicated `Init FTP` and `Test Integration FTP` steps on Ubuntu so the workflow cannot claim FTP verification coverage without actually running the real suite |
| T-04-06 | D | FTP CI provisioning | accept | FTP backend setup remains Linux-only in CI for parity with SFTP/MinIO; this is accepted because cross-platform standard tests still run elsewhere and backend service setup already follows this constraint |
</threat_model>

<verification>
- `go test -v -race -tags=integration_test_ftp ./integration -run TestIntegration_FTP -count=1`
- Confirm `.github/workflows/go.yml` contains `Init FTP` and `Test Integration FTP` Ubuntu-only steps.
</verification>

<success_criteria>
- FTP push and pull verification run as a dedicated tagged integration suite.
- CI provisions the FTP test environment and executes that suite.
- Real-server FTP coverage becomes part of the repository’s standard protocol verification matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ftp-verification-discoverability/04-02-SUMMARY.md`
</output>
