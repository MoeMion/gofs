---
phase: 04-ftp-verification-discoverability
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README-CN.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "Users can find a working FTP push example in the main README surfaces"
    - "Users can find a working FTP pull example in the main README surfaces"
    - "Users can clearly see FTP v1 limitations near the examples and in a dedicated limitations list"
  artifacts:
    - path: README.md
      provides: English FTP usage examples and limitations
    - path: README-CN.md
      provides: Chinese FTP usage examples and limitations
  key_links:
    - from: README.md
      to: core/vfs.go
      via: documented FTP endpoint grammar
      pattern: "ftp_user|ftp_pass|ftp_passive"
    - from: README-CN.md
      to: README.md
      via: mirrored protocol discoverability
      pattern: "FTP"
---

<objective>
Make FTP discoverable and honestly scoped in the same user-facing surfaces as SFTP and MinIO, per D-07 through D-12.

Purpose: Even with real tests, Phase 4 fails if users cannot find FTP usage or understand its v1 limits.
Output: FTP push/pull examples plus explicit limitations text in both `README.md` and `README-CN.md`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ftp-verification-discoverability/04-CONTEXT.md
@.planning/phases/04-ftp-verification-discoverability/04-RESEARCH.md
@README.md
@README-CN.md
@.planning/phases/01-ftp-endpoint-contract-routing/01-CONTEXT.md
@.planning/phases/02-ftp-driver-backend/02-CONTEXT.md
@.planning/phases/03-one-way-ftp-sync-flows/03-CONTEXT.md

<interfaces>
Canonical FTP endpoint parameters already shipped and must be documented verbatim:

```text
ftp://127.0.0.1:21?remote_path=/path&ftp_user=ftp_user&ftp_pass=ftp_pwd&ftp_passive=true
```

The docs must reflect these user-visible constraints from prior phases:

```text
plain FTP only
passive mode only in v1
no FTP↔FTP sync
backend capability failures surface explicitly
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add first-class FTP push and pull usage examples to both READMEs</name>
  <read_first>
    - README.md
    - README-CN.md
    - .planning/phases/01-ftp-endpoint-contract-routing/01-CONTEXT.md
    - .planning/phases/04-ftp-verification-discoverability/04-CONTEXT.md
  </read_first>
  <files>README.md, README-CN.md</files>
  <acceptance_criteria>
    - `README.md` contains `### FTP Push Client`
    - `README.md` contains `### FTP Pull Client`
    - `README.md` contains `ftp_user=` and `ftp_passive=true`
    - `README-CN.md` contains `### FTP推送客户端`
    - `README-CN.md` contains `### FTP拉取客户端`
    - `README-CN.md` contains `ftp_user=` and `ftp_passive=true`
  </acceptance_criteria>
  <action>Update `README.md` and `README-CN.md` to add FTP as a first-class protocol alongside SFTP and MinIO per D-07, D-08, and D-09. Add an English `### FTP Push Client` example and `### FTP Pull Client` example near the existing SFTP/MinIO sections, then add matching Chinese sections `### FTP推送客户端` and `### FTP拉取客户端`. Use the actual endpoint grammar from Phase 1, including `ftp_user`, `ftp_pass`, `ftp_passive=true`, and `remote_path`; do not invent userinfo-style `ftp://user:pass@host/...` examples. Include both push (`-source="./source" -dest="ftp://..."`) and pull (`-source="ftp://..." -dest="./dest" -sync_once`) directions so users can discover both supported workflows.</action>
  <verify>
    <automated>go test ./core -run TestVFSParseFTP -count=1</automated>
  </verify>
  <done>Both README surfaces now expose working FTP push and pull examples with the actual shipped endpoint grammar.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit FTP v1 limitations near examples and in a dedicated list</name>
  <read_first>
    - README.md
    - README-CN.md
    - .planning/phases/02-ftp-driver-backend/02-CONTEXT.md
    - .planning/phases/03-one-way-ftp-sync-flows/03-CONTEXT.md
    - .planning/phases/04-ftp-verification-discoverability/04-CONTEXT.md
  </read_first>
  <files>README.md, README-CN.md</files>
  <acceptance_criteria>
    - `README.md` contains `plain FTP only`
    - `README.md` contains `active mode`
    - `README.md` contains `no FTP↔FTP sync`
    - `README.md` contains `capability failures`
    - `README-CN.md` contains `仅支持明文 FTP` or equivalent explicit plain-FTP-only wording
    - `README-CN.md` contains `不支持主动模式` or equivalent explicit active-mode wording
    - `README-CN.md` contains `不支持 FTP↔FTP` or equivalent explicit FTP-to-FTP wording
  </acceptance_criteria>
  <action>Add FTP v1 limitations text in both `README.md` and `README-CN.md` per D-10, D-11, and D-12. Put a short limitation note directly below the FTP usage examples so readers cannot miss it, and also add a dedicated FTP limitations bullet list in the same protocol docs area. The limitations must explicitly state all of the following: plain FTP only in v1, no FTPS, active mode is unsupported in v1, no FTP↔FTP sync, and backend capability failures are surfaced explicitly rather than silently ignored. Keep the wording aligned with the current README tone, but do not soften or imply these boundaries indirectly — they must be direct and discoverable.</action>
  <verify>
    <automated>go test ./core -run TestVFSParseFTP -count=1</automated>
  </verify>
  <done>Users can see FTP’s supported usage and its major v1 boundaries immediately in both English and Chinese docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → user configuration decisions | Users rely on README examples and limitations to decide whether FTP fits their use case |
| prior shipped behavior → current docs | Documentation must not contradict the actual FTP endpoint grammar or capability limits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | I | `README.md` / `README-CN.md` | mitigate | Document the exact `ftp_*` endpoint grammar and both push/pull examples so users are not forced to infer configuration from unrelated protocols |
| T-04-06 | R | FTP limitations text | mitigate | State plain FTP only, passive-only v1 behavior, no FTP↔FTP sync, and explicit capability failures directly near examples and in a dedicated list |
| T-04-07 | T | bilingual docs parity | mitigate | Mirror FTP discoverability additions in `README-CN.md` so one README cannot drift into an unsupported or incomplete story |
</threat_model>

<verification>
- `go test ./core -run TestVFSParseFTP -count=1`
- Confirm both READMEs contain FTP push/pull sections and explicit limitations text.
</verification>

<success_criteria>
- FTP is discoverable in the same protocol documentation tier as SFTP and MinIO.
- Both push and pull examples are documented with the real endpoint grammar.
- FTP v1 limits are explicit enough that users are not surprised by unsupported scope.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ftp-verification-discoverability/04-03-SUMMARY.md`
</output>
