---
phase: 05-public-ftpsyncservice-api-contract
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [ftpsync/hooks.go, ftpsync/service.go, ftpsync/hooks_test.go]
autonomous: true
requirements: [API-05]
must_haves:
  truths:
    - "Developer can attach optional logging, progress, and sync event hooks."
    - "Default service construction remains no-op and library-local when hooks are omitted."
    - "Hook execution never requires global loggers, web reports, report.Reporter, or eventlog runtime packages."
  artifacts:
    - path: "ftpsync/hooks.go"
      provides: "Public hook function types and no-op defaults"
      exports: ["Logger", "ProgressHook", "EventHook", "SyncEvent", "HookSet"]
    - path: "ftpsync/service.go"
      provides: "Service-owned hook normalization and dispatch helpers"
      contains: "normalizeHooks"
    - path: "ftpsync/hooks_test.go"
      provides: "Hook no-op/default and custom callback coverage"
  key_links:
    - from: "ftpsync/service.go"
      to: "ftpsync/hooks.go"
      via: "constructor normalizes nil hooks to no-op functions stored on the service"
      pattern: "normalizeHooks|HookSet"
    - from: "ftpsync/hooks_test.go"
      to: "ftpsync/service.go"
      via: "custom hooks invoked only by explicit service helper/method paths"
      pattern: "ProgressHook|EventHook"
---

<objective>
Add optional logging, progress, and sync-event hooks to the public API while preserving no-op defaults.

Purpose: Library callers need observability extension points without inheriting the old global logger/report/web runtime.
Output: Hook types, service-owned no-op normalization, and tests proving hooks are optional and isolated from legacy reporting.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/05-public-ftpsyncservice-api-contract/05-01-SUMMARY.md
@.planning/phases/05-public-ftpsyncservice-api-contract/05-02-SUMMARY.md

<interfaces>
Existing runtime logger/reporter packages must not be exposed by the public `ftpsync` API:

```go
// logger.Logger is internal runtime logging and must not appear in public ftpsync hooks.
type Logger struct { log.Logger; Sample log.Logger }

// report.Reporter is old web/report runtime and must not be imported by ftpsync.
type Reporter interface { GetReport() Report; PutEvent(event eventlog.Event); Enable(enabled bool) }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define library-local hook contracts</name>
  <files>ftpsync/hooks.go, ftpsync/hooks_test.go</files>
  <behavior>
    - Test 1: callers can provide logging, progress, and event hooks using only `ftpsync` and standard-library types.
    - Test 2: hook types do not expose `logger.Logger`, `report.Reporter`, `eventlog.Event`, web reports, or global logger instances.
    - Test 3: `HookSet` zero value can be normalized without panics.
  </behavior>
  <action>Create `ftpsync/hooks.go` with exported hook contracts: a small `Logger` interface or function type limited to library messages, `ProgressHook` for byte/file progress snapshots, `EventHook` for sync event notifications, `SyncEvent` value type for operation/path/status/error-kind metadata, and `HookSet` containing optional hooks. Use only standard library imports and `ftpsync` package types. Do not import or expose `github.com/no-src/gofs/logger`, `report`, `eventlog`, Gin, web reports, or global loggers.</action>
  <verify>
    <automated>go test ./ftpsync -run TestHookContracts -count=1</automated>
  </verify>
  <done>Public hook contracts compile from consumer code, use no legacy runtime types, and cover logging, progress, and sync events for API-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize no-op defaults and prove optional hook behavior</name>
  <files>ftpsync/service.go, ftpsync/hooks_test.go</files>
  <behavior>
    - Test 1: constructing `FTPSyncService` with no hooks succeeds and calling internal dispatch helpers does nothing observable and does not panic.
    - Test 2: custom logging hook receives explicit library messages when invoked by service dispatch helpers.
    - Test 3: custom progress and event hooks receive typed progress/event payloads when invoked by service dispatch helpers.
    - Test 4: package dependency boundary still excludes `report`, `eventlog`, `server`, and global logger packages from public hook implementation.
  </behavior>
  <action>Extend `Options`/`FTPSyncService` storage to include `HookSet` from Plan 01/03, add private `normalizeHooks` and small service dispatch helpers for log/progress/event notifications. Nil hooks must become no-op functions. Tests may exercise dispatch helpers through public method behavior or package-local tests; do not add global state. Keep hooks library-local and optional so default service construction remains no-op per API-05.</action>
  <verify>
    <automated>go test ./ftpsync -run 'Test(HookDefaults|CustomHooks|PackageDependencyBoundary)' -count=1</automated>
  </verify>
  <done>Omitted hooks are safe no-ops, custom hooks receive typed callbacks, and `ftpsync` still does not depend on global logger/report/web runtime packages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ftpsync→caller hooks | Library-controlled events call caller-provided functions. |
| caller hooks→library method path | Caller hook panics or slow hooks can affect sync method flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | I | hook payloads | mitigate | Do not include FTP passwords in hook payload structs or dispatch helpers; tests should use a sample password and assert it is not emitted. |
| T-05-03-02 | D | caller hook execution | accept | Hooks are in-process caller code; document that hooks run synchronously in the caller's process, and avoid adding goroutines/global queues in this phase. |
| T-05-03-03 | E | legacy report/logger imports | mitigate | Dependency-boundary test rejects `report`, `eventlog`, `server`, and `logger` imports from `ftpsync`. |
</threat_model>

<verification>
Run `go test ./ftpsync -count=1` and `go list -deps ./ftpsync` dependency checks from Plan 01/03. Then run `go test ./... -run TestNonExistent -count=1` to compile all packages without executing long integration suites.
</verification>

<success_criteria>
- API-05 is covered: logging, progress, and event hooks are public, optional, and no-op by default.
- Hooks do not expose or require global logger, web report, eventlog, or old runtime packages.
- All Phase 5 public API tests pass for construction, validation, context/error classification, and hook contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-ftpsyncservice-api-contract/05-03-SUMMARY.md`
</output>
