---
phase: 06-one-shot-disk-ftp-sync-through-library-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ftpsync/service.go, ftpsync/errors.go, ftpsync/oneshot.go, ftpsync/oneshot_test.go]
autonomous: true
requirements: [ONCE-05]
must_haves:
  truths:
    - "Developer receives a compact one-shot result summary from SyncOnce instead of relying on terminal output."
    - "Partial success returns both Result and a non-nil typed transfer error."
    - "SyncOnce stays on the existing ftpsync typed API boundary without reviving CLI/config/runtime entrypoints."
  artifacts:
    - path: "ftpsync/oneshot.go"
      provides: "Library-local one-shot adapter, result aggregation, and partial-failure execution scaffolding"
      exports: ["internal adapter only — no new public constructor"]
    - path: "ftpsync/service.go"
      provides: "SyncOnce dispatch into real execution instead of unsupported placeholder"
      contains: "func (s *FTPSyncService) SyncOnce(ctx context.Context)"
    - path: "ftpsync/oneshot_test.go"
      provides: "Public result and partial-failure coverage"
  key_links:
    - from: "ftpsync/service.go"
      to: "ftpsync/oneshot.go"
      via: "SyncOnce delegates to library-local executeSyncOnce helper after validation/context checks"
      pattern: "executeSyncOnce|syncOnce"
    - from: "ftpsync/oneshot.go"
      to: "sync.NewSync"
      via: "internal adapter builds legacy syncer from typed options"
      pattern: "NewSync"
---

<objective>
Define the Phase 6 one-shot execution contract behind `FTPSyncService`.

Purpose: Per D-01 through D-05, Phase 6 needs a compact result summary and typed partial-failure behavior before the direction-specific push/pull implementations are wired in.
Output: Real `SyncOnce` execution scaffolding, compact `Result` fields, partial-failure error handling, and a library-local adapter that can build the legacy FTP sync engine from typed options.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-one-shot-disk-ftp-sync-through-library-api/06-CONTEXT.md
@.planning/phases/06-one-shot-disk-ftp-sync-through-library-api/06-RESEARCH.md
@.planning/phases/06-one-shot-disk-ftp-sync-through-library-api/06-PATTERNS.md
@.planning/phases/05-public-ftpsyncservice-api-contract/05-02-SUMMARY.md
@.planning/phases/05-public-ftpsyncservice-api-contract/05-03-SUMMARY.md
@.planning/debug/sync-files-copied-to-cwd.md

<interfaces>
From `ftpsync/service.go`:

```go
type Result struct {
	Direction Direction
}

func (s *FTPSyncService) SyncOnce(ctx context.Context) (Result, error)
```

From `sync/sync.go`:

```go
type Sync interface {
	Create(path string) error
	Symlink(oldname, newname string) error
	Write(path string) error
	Remove(path string) error
	Rename(path string) error
	Chmod(path string) error
	IsDir(path string) (bool, error)
	SyncOnce(path string) error
	Source() core.VFS
	Dest() core.VFS
	Close()
}
```

From `driver/driver.go`:

```go
type Driver interface {
	Connect() error
	WalkDir(root string, fn fs.WalkDirFunc) error
	Open(path string) (http.File, error)
	Stat(path string) (fs.FileInfo, error)
	GetFileTime(path string) (time.Time, time.Time, time.Time, error)
	Write(src string, dest string) error
	ReadLink(path string) (string, error)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the SyncOnce placeholder with a real one-shot contract per D-01 through D-05</name>
  <files>ftpsync/service.go, ftpsync/errors.go, ftpsync/oneshot.go, ftpsync/oneshot_test.go</files>
  <read_first>ftpsync/service.go, ftpsync/errors.go, ftpsync/context_test.go, .planning/phases/06-one-shot-disk-ftp-sync-through-library-api/06-CONTEXT.md, .planning/phases/06-one-shot-disk-ftp-sync-through-library-api/06-RESEARCH.md</read_first>
  <acceptance_criteria>`ftpsync/service.go` no longer contains `unsupportedMethod("SyncOnce"`; `ftpsync/service.go` exports `Result` fields for direction plus compact summary data; `ftpsync/oneshot.go` contains an execution helper invoked by `SyncOnce`; `go test ./ftpsync -run 'TestSyncOnce(Result|Partial|Hooks)' -count=1` exits 0.</acceptance_criteria>
  <behavior>
    - Test 1: successful one-shot execution returns a compact summary Result instead of a zero-value placeholder.
    - Test 2: when some operations fail and some succeed, SyncOnce returns both Result and a non-nil typed transfer error per D-04/D-05.
    - Test 3: the summary remains compact and does not expose a per-file report slice per D-01/D-02.
  </behavior>
  <action>Add `ftpsync/oneshot.go` and move one-shot orchestration there. Expand `Result` in `ftpsync/service.go` to include `Direction`, `SourceRoot`, `DestinationRoot`, `StartedAt`, `CompletedAt`, `PathsVisited`, `FilesAttempted`, `DirectoriesAttempted`, `FailureCount`, and `Partial` so the contract stays summary-oriented per D-01/D-02. Add a typed transfer/partial-failure path in `ftpsync/errors.go` that keeps `ErrTransfer` classification while preserving the summary return per D-03/D-05. Change `SyncOnce(ctx)` so, after existing validation and context checks, it calls a real execution helper rather than `unsupportedMethod`.</action>
  <verify>
    <automated>go test ./ftpsync -run 'TestSyncOnce(Result|Partial|Hooks)' -count=1</automated>
  </verify>
  <done>Public callers get a useful compact `Result` from `SyncOnce`, and partial-failure runs return `Result + error` instead of losing the summary or stopping at an unsupported placeholder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the legacy FTP sync adapter from typed public options without changing the public API per D-06 and D-12</name>
  <files>ftpsync/oneshot.go, ftpsync/oneshot_test.go</files>
  <read_first>ftpsync/options.go, sync/sync.go, sync/option.go, core/vfs.go, ignore/path_ignore.go, retry/default_retry.go, logger/logger.go</read_first>
  <acceptance_criteria>`ftpsync/oneshot.go` contains helper code that constructs `core.NewDiskVFS(...)` and internal `core.NewVFS("ftp://...")` values; `ftpsync/oneshot.go` contains a seam wrapping `sync.NewSync`; no new public types/options are added to `ftpsync/options.go`; `go test ./ftpsync -run 'Test(NewFTPSyncService|SyncOnceBuildsLegacyAdapter)' -count=1` exits 0.</acceptance_criteria>
  <behavior>
    - Test 1: local→FTP typed options produce an internal disk source plus FTP destination that routes into `sync.NewSync`.
    - Test 2: FTP→local typed options produce an internal FTP source plus disk destination that routes into `sync.NewSync`.
    - Test 3: ignore rules, retry settings, and no-op logger/hooks are built library-locally rather than via CLI/config parsing.
  </behavior>
  <action>In `ftpsync/oneshot.go`, add library-local builders that convert typed `Options` into the existing internal execution dependencies: `core.NewDiskVFS(opts.Source.LocalPath)` or `core.NewDiskVFS(opts.Destination.LocalPath)` for disk endpoints; `core.NewVFS("ftp://{host}:{port}?remote_path={remote}&ftp_user={user}&ftp_pass={password}&ftp_passive={true|false}&ftp_timeout={duration}&ftp_encoding={encoding}")` for FTP endpoints; `retry.New(opts.Retry.Count, opts.Retry.Wait, opts.Retry.Async, logger.NewEmptyLogger())`; and a library-local `ignore.PathIgnore` adapter for typed `IgnoreRule` values. Keep this URL construction internal only — do not add parser strings or policy knobs to the public API per D-06 and D-12.</action>
  <verify>
    <automated>go test ./ftpsync -run 'Test(NewFTPSyncService|SyncOnceBuildsLegacyAdapter)' -count=1</automated>
  </verify>
  <done>`ftpsync` can build the legacy execution engine from typed public options while preserving the existing Phase 5 API surface and avoiding any CLI/config/runtime re-entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→`SyncOnce` | Untrusted caller input controls endpoints, retry values, ignore patterns, and cancellation. |
| `ftpsync`→legacy sync engine | Typed public options are converted into internal VFS/sync dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | I | partial-failure/result errors | mitigate | Keep passwords out of all `ErrTransfer`/partial-failure messages; verify tests with sample secrets assert absence from `err.Error()`. |
| T-06-01-02 | T | internal adapter builder | mitigate | Build `core.VFS`/`sync.Option` only from validated typed options; do not reintroduce YAML/CLI parser input paths. |
| T-06-01-03 | D | `SyncOnce` context handling | mitigate | Preserve `validateContext` ordering so canceled contexts fail before adapter construction or network work starts. |
| T-06-01-04 | R | result summary | mitigate | Include start/end timestamps and compact attempt/failure counts so library callers can log or retry with observable evidence. |
</threat_model>

<verification>
Run `go test ./ftpsync -count=1` after the plan. Confirm `ftpsync/service.go` no longer returns `ErrUnsupportedCapability` for `SyncOnce`, while `StartBackground` remains unchanged for Phase 7.
</verification>

<success_criteria>
- ONCE-05 is covered with a compact, structured `Result` contract.
- D-03/D-04/D-05 are reflected in the public one-shot error/result behavior.
- No new public policy options or runtime surfaces are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/06-one-shot-disk-ftp-sync-through-library-api/06-01-SUMMARY.md`
</output>
