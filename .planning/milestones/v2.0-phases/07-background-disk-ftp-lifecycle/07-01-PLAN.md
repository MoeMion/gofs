---
phase: 07-background-disk-ftp-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ftpsync/service.go, ftpsync/background.go, ftpsync/background_test.go, ftpsync/context_test.go]
autonomous: true
requirements: [WATCH-01, WATCH-03, WATCH-05]
must_haves:
  truths:
    - "Developer can start background local disk→FTP sync through FTPSyncService without invoking the CLI per D-07 and D-12."
    - "Developer receives a lifecycle handle with readiness/wait/error semantics strong enough for long-lived embedders per D-08, D-09, and D-10."
    - "Developer cannot start FTP→disk background polling through the v2.0 public API per D-11."
  artifacts:
    - path: "ftpsync/service.go"
      provides: "Public StartBackground dispatch and expanded Handle contract"
      contains: "func (s *FTPSyncService) StartBackground(ctx context.Context) (Handle, error)"
    - path: "ftpsync/background.go"
      provides: "Library-local background runner and concrete handle"
    - path: "ftpsync/background_test.go"
      provides: "Background startup and handle contract regression coverage"
  key_links:
    - from: "ftpsync/service.go"
      to: "ftpsync/background.go"
      via: "StartBackground delegates to executeStartBackground after validation/context checks"
      pattern: "executeStartBackground|StartBackground"
    - from: "ftpsync/background.go"
      to: "ftpsync/oneshot.go"
      via: "initial sync reuses the existing local→FTP one-shot orchestration"
      pattern: "executeSyncOnce|runSyncOnceLocalToFTP"
---

<objective>
Create the Phase 7 public background lifecycle entrypoint and handle contract.

Purpose: WATCH-01, WATCH-03, and WATCH-05 require the public API to start only supported local→FTP background sync, return a real lifecycle handle, and keep FTP→local background mode unavailable.
Output: `StartBackground(ctx)` dispatch, a concrete background handle, and tests that lock the public contract before deeper watcher behavior is added.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-background-disk-ftp-lifecycle/07-CONTEXT.md
@.planning/phases/07-background-disk-ftp-lifecycle/07-RESEARCH.md
@.planning/phases/07-background-disk-ftp-lifecycle/07-PATTERNS.md
@.planning/phases/05-public-ftpsyncservice-api-contract/05-02-SUMMARY.md
@.planning/phases/06-one-shot-disk-ftp-sync-through-library-api/06-02-SUMMARY.md

<interfaces>
From `ftpsync/service.go`:

```go
type Handle interface {
	Done() <-chan struct{}
	Err() error
	Stop(context.Context) error
}

func (s *FTPSyncService) StartBackground(ctx context.Context) (Handle, error)
```

From `ftpsync/oneshot.go`:

```go
func executeSyncOnce(ctx context.Context, svc *FTPSyncService) (Result, error)
```

From `retry/retry.go`:

```go
type Retry interface {
	Do(f func() error, desc string) wait.Wait
	DoWithContext(ctx context.Context, f func() error, desc string) wait.Wait
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the StartBackground unsupported stub with the Phase 7 public lifecycle contract per D-07, D-08, D-09, and D-11</name>
  <files>ftpsync/service.go, ftpsync/background.go, ftpsync/background_test.go, ftpsync/context_test.go</files>
  <read_first>ftpsync/service.go, ftpsync/context_test.go, ftpsync/oneshot.go, .planning/phases/07-background-disk-ftp-lifecycle/07-CONTEXT.md, .planning/phases/05-public-ftpsyncservice-api-contract/05-02-SUMMARY.md</read_first>
  <acceptance_criteria>`ftpsync/service.go` no longer returns `unsupportedMethod("StartBackground"` for `DirectionLocalToFTP`; `ftpsync/service.go` exports `Wait() error` on `Handle`; `ftpsync/background.go` contains `executeStartBackground`; `go test ./ftpsync -run 'Test(StartBackgroundChecksValidationAndContext|StartBackgroundRejectsFTPToLocal|BackgroundHandleWait)' -count=1` exits 0.</acceptance_criteria>
  <behavior>
    - Test 1: local→FTP `StartBackground(context.Background())` returns a non-nil handle instead of unsupported capability.
    - Test 2: FTP→local `StartBackground` still returns `ErrUnsupportedCapability` with method/direction context per D-11.
    - Test 3: the public handle exposes `Done()`, `Err()`, `Wait()`, and `Stop(context.Context)` so embedders can observe shutdown per D-09/D-10.
  </behavior>
  <action>Update `ftpsync/service.go` so `Handle` includes `Wait() error` in addition to `Done()`, `Err()`, and `Stop(context.Context) error`. Change `StartBackground(ctx)` to preserve current validation/context ordering, return `ErrUnsupportedCapability` for every direction except `DirectionLocalToFTP` per D-11, and delegate supported runs to a new `executeStartBackground(ctx, svc)` helper in `ftpsync/background.go`. In `ftpsync/background.go`, add a concrete handle type with mutex-protected current/final error state, `done` signaling, and `stopOnce` semantics suitable for long-lived service wrappers per D-08/D-10.</action>
  <verify>
    <automated>go test ./ftpsync -run 'Test(StartBackgroundChecksValidationAndContext|StartBackgroundRejectsFTPToLocal|BackgroundHandleWait)' -count=1</automated>
  </verify>
  <done>Local→FTP background startup is publicly reachable, FTP→local background remains blocked, and callers receive a real lifecycle handle with explicit wait semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run initial catch-up before readiness and expose deterministic startup signals per D-01 and D-02</name>
  <files>ftpsync/background.go, ftpsync/background_test.go</files>
  <read_first>ftpsync/background.go, ftpsync/oneshot.go, ftpsync/hooks.go, .planning/phases/07-background-disk-ftp-lifecycle/07-RESEARCH.md</read_first>
  <acceptance_criteria>`ftpsync/background.go` runs an initial local→FTP sync before marking the handle ready; readiness is observable through `Wait()`/`Done()` behavior or an internal ready barrier used by tests; `go test ./ftpsync -run 'TestStartBackgroundInitialSyncBeforeReady' -count=1` exits 0.</acceptance_criteria>
  <behavior>
    - Test 1: `StartBackground` triggers an initial local→FTP sync pass before watch-mode readiness.
    - Test 2: startup prefers remote correctness over fastest watch startup by not signaling readiness before initial catch-up completes.
    - Test 3: initial sync failures are observable through handle error state but do not inherently violate the long-running contract when the runner can continue.
  </behavior>
  <action>In `ftpsync/background.go`, structure startup as `initial sync -> watcher setup -> ready`. Reuse the existing local→FTP execution path from `ftpsync/oneshot.go` for the initial catch-up rather than inventing a second transfer path. Add an internal readiness barrier so tests can assert that the first sync ran before the background runner begins steady-state watching. Keep failure handling aligned with D-03/D-04 by storing the error on the handle instead of crashing the process-level contract.</action>
  <verify>
    <automated>go test ./ftpsync -run 'TestStartBackgroundInitialSyncBeforeReady' -count=1</automated>
  </verify>
  <done>The background run starts with a real catch-up sync before watch mode, matching the locked lifecycle behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→`StartBackground` | Untrusted caller input controls direction, local path, FTP credentials, cancellation, and shutdown timing. |
| `ftpsync` lifecycle handle→caller | Caller depends on handle signals to coordinate service shutdown and error observation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | T | `StartBackground` direction dispatch | mitigate | Explicitly allow only `DirectionLocalToFTP`; keep `DirectionFTPToLocal` returning `ErrUnsupportedCapability` with method/direction context. |
| T-07-01-02 | D | lifecycle handle API | mitigate | Add `Wait()` and deterministic `Done()` closure so callers do not block indefinitely or poll blindly during shutdown. |
| T-07-01-03 | R | startup sequence | mitigate | Run initial sync before readiness and store the latest/final error on the handle so embedders can reason about startup state. |
</threat_model>

<verification>
Run `go test ./ftpsync -run 'Test(StartBackgroundChecksValidationAndContext|StartBackgroundRejectsFTPToLocal|BackgroundHandleWait|StartBackgroundInitialSyncBeforeReady)' -count=1`.
</verification>

<success_criteria>
- WATCH-01 public entrypoint exists for local→FTP.
- WATCH-03 handle contract includes wait/error/stop semantics.
- WATCH-05 remains enforced for FTP→local background mode.
</success_criteria>

<output>
After completion, create `.planning/phases/07-background-disk-ftp-lifecycle/07-01-SUMMARY.md`
</output>
