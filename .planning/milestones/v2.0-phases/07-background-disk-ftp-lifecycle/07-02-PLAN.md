---
phase: 07-background-disk-ftp-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [ftpsync/background.go, ftpsync/background_test.go]
autonomous: true
requirements: [WATCH-01, WATCH-02, WATCH-03]
must_haves:
  truths:
    - "Local create, update, delete, and rename-relevant changes are applied while the background run is active per D-05 and D-06."
    - "Bursty local changes coalesce into bounded sync passes instead of redundant uploads per D-05 and D-06."
    - "Sync pass failures are observable but do not terminate the background run by default per D-03 and D-04."
  artifacts:
    - path: "ftpsync/background.go"
      provides: "fsnotify watcher registration, debounce loop, and repeated sync triggering"
    - path: "ftpsync/background_test.go"
      provides: "event batching, new directory watch coverage, and non-terminal failure tests"
  key_links:
    - from: "ftpsync/background.go"
      to: "monitor/fsnotify_monitor.go"
      via: "recursive directory watch registration and create-event directory watching pattern"
      pattern: "WalkDir|watcher.Add|Create"
    - from: "ftpsync/background.go"
      to: "ftpsync/oneshot.go"
      via: "coalesced event bursts trigger repeated local→FTP sync passes rather than per-event FTP mutations"
      pattern: "executeSyncOnce|runSyncOnceLocalToFTP"
---

<objective>
Add the watcher and debounce behavior for steady-state disk→FTP background sync.

Purpose: WATCH-02 requires real local filesystem change detection, while D-05/D-06 require coalescing rather than immediate per-event upload.
Output: recursive fsnotify watch coverage, debounced background sync triggers, and tests for burst handling and continued operation after sync errors.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-background-disk-ftp-lifecycle/07-CONTEXT.md
@.planning/phases/07-background-disk-ftp-lifecycle/07-RESEARCH.md
@.planning/phases/07-background-disk-ftp-lifecycle/07-PATTERNS.md
@.planning/phases/07-background-disk-ftp-lifecycle/07-01-PLAN.md
@monitor/fsnotify_monitor.go
@monitor/base_monitor.go

<interfaces>
From `monitor/fsnotify_monitor.go`:

```go
watcher, err := fsnotify.NewWatcher()
err = filepath.WalkDir(dir, func(path string, d fs.DirEntry, err error) error {
	if d.IsDir() {
		watcher.Add(path)
	}
	return err
})
```

From `monitor/base_monitor.go`:

```go
if (wm.count <= 2 && now-wm.last <= time.Second.Nanoseconds()) || (wm.count > 2 && now-wm.last <= 3*time.Second.Nanoseconds()) {
	continue
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Watch the source tree and debounce bursty events into coalesced sync passes per D-05 and D-06</name>
  <files>ftpsync/background.go, ftpsync/background_test.go</files>
  <read_first>ftpsync/background.go, monitor/fsnotify_monitor.go, monitor/base_monitor.go, ftpsync/oneshot.go</read_first>
  <acceptance_criteria>`ftpsync/background.go` creates an `fsnotify.Watcher`, recursively watches the configured source root, resets a debounce timer on event bursts, and schedules at most one pending sync trigger per burst; `go test ./ftpsync -run 'TestBackgroundDebouncesBurstEvents|TestBackgroundWatchesNewDirectories' -count=1` exits 0.</acceptance_criteria>
  <behavior>
    - Test 1: multiple local events inside the debounce window produce one follow-up sync pass, not N FTP upload cycles.
    - Test 2: newly created directories are added to watcher coverage so later nested writes are observed.
    - Test 3: create/write/remove/rename-relevant events mark the source tree dirty for a coalesced resync.
  </behavior>
  <action>In `ftpsync/background.go`, add recursive watcher registration for the configured local source root using `filepath.WalkDir` and `fsnotify.NewWatcher`. Handle `Create`, `Write`, `Remove`, and `Rename` as dirtying events. When a created path is a directory, add it to watcher coverage immediately. Use a single buffered trigger channel plus resettable debounce timer so a burst of events collapses into one queued sync pass per D-05/D-06 instead of calling FTP mutations for each raw event.</action>
  <verify>
    <automated>go test ./ftpsync -run 'TestBackgroundDebouncesBurstEvents|TestBackgroundWatchesNewDirectories' -count=1</automated>
  </verify>
  <done>The runner observes ongoing local changes and coalesces bursty filesystem noise into bounded background sync passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep sync failures observable but non-terminal during steady-state operation per D-03 and D-04</name>
  <files>ftpsync/background.go, ftpsync/background_test.go</files>
  <read_first>ftpsync/background.go, ftpsync/errors.go, ftpsync/hooks.go, retry/retry.go, .planning/phases/07-background-disk-ftp-lifecycle/07-RESEARCH.md</read_first>
  <acceptance_criteria>`ftpsync/background.go` records sync pass failures on the handle without closing `Done()` prematurely; subsequent queued sync passes still execute after an error; `go test ./ftpsync -run 'TestBackgroundSyncFailureIsObservableAndNonTerminal' -count=1` exits 0.</acceptance_criteria>
  <behavior>
    - Test 1: one failed sync pass updates handle error state.
    - Test 2: the runner stays alive and processes a later sync pass after the failure.
    - Test 3: failure visibility uses existing compact error/hook semantics rather than crashing the background run.
  </behavior>
  <action>Make the steady-state sync worker call the same local→FTP orchestration path used for initial catch-up, but on failure store the latest error on the handle, emit hook/log notifications, and continue processing later triggers per D-03/D-04. Do not close the handle or stop the watcher merely because one sync cycle returns `ErrTransfer`, `ErrConnection`, or `ErrAuthentication`.</action>
  <verify>
    <automated>go test ./ftpsync -run 'TestBackgroundSyncFailureIsObservableAndNonTerminal' -count=1</automated>
  </verify>
  <done>Background sync behaves like a long-running service: failures are visible, but the run continues unless explicitly stopped or canceled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local fsnotify stream→background runner | Untrusted and noisy platform file events are converted into sync work. |
| background runner→FTP sync engine | Coalesced triggers dispatch network-affecting sync passes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | D | event burst handling | mitigate | Use a debounce timer plus single pending trigger channel so bursts do not spawn unbounded concurrent sync passes. |
| T-07-02-02 | T | directory watch coverage | mitigate | Add newly created directories to watcher coverage immediately so nested paths are not silently missed. |
| T-07-02-03 | R | sync failure lifecycle | mitigate | Persist latest error on the handle and keep the runner alive, providing observable evidence without silently dropping the service. |
</threat_model>

<verification>
Run `go test ./ftpsync -run 'TestBackgroundDebouncesBurstEvents|TestBackgroundWatchesNewDirectories|TestBackgroundSyncFailureIsObservableAndNonTerminal' -count=1`.
</verification>

<success_criteria>
- WATCH-02 is covered with recursive watch + coalesced resync behavior.
- D-05/D-06 are reflected in event batching rather than per-event FTP mutation.
- D-03/D-04 are reflected in non-terminal steady-state failure behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-background-disk-ftp-lifecycle/07-02-SUMMARY.md`
</output>
