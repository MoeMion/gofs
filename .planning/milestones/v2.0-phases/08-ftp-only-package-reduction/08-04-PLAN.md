---
phase: 08-ftp-only-package-reduction
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - go.mod
  - go.sum
  - ftpsync/dependency_boundary_test.go
  - .github/workflows/go.yml
autonomous: true
requirements: [PRUNE-01, PRUNE-02, PRUNE-03, PRUNE-04]
must_haves:
  truths:
    - "`go.mod` and `go.sum` no longer retain unnecessary runtime dependencies."
    - "`go test ./...` passes for the reduced root module."
    - "`go list -deps ./...` passes the explicit blacklist from D-11."
  artifacts:
    - path: "go.mod"
      provides: "Tidied dependency set for FTP sync library"
      contains: "github.com/jlaffaye/ftp"
    - path: "ftpsync/dependency_boundary_test.go"
      provides: "Final dependency proof"
      contains: "TestPackageDependencyBoundaryRejectsOldRuntime"
  key_links:
    - from: "go.mod"
      to: "go list -deps ./..."
      via: "module dependency resolution"
      pattern: "go list -deps ./..."
---

<objective>
Tidy the reduced module and prove the final build graph is FTP-only.

Purpose: PRUNE-04 and D-13 require module cleanup as evidence, while D-10/D-11 require explicit dependency graph proof. This plan makes Phase 8 verifiable end-to-end.
Output: Tidied `go.mod`/`go.sum`, updated CI test workflow if needed, and passing final blacklist/test commands.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ftp-only-package-reduction/08-CONTEXT.md
@.planning/phases/08-ftp-only-package-reduction/08-03-SUMMARY.md
@go.mod
@go.sum
@ftpsync/dependency_boundary_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tidy module dependencies and update CI test scope</name>
  <files>go.mod, go.sum, .github/workflows/go.yml</files>
  <read_first>go.mod, go.sum, .github/workflows/go.yml, .planning/phases/08-ftp-only-package-reduction/08-CONTEXT.md</read_first>
  <action>Run `go mod tidy` after old runtime deletion. Keep required dependencies for the retained library only: `github.com/jlaffaye/ftp` for FTP client behavior, `github.com/fsnotify/fsnotify` for approved background local disk→FTP monitoring, and standard transitive dependencies required by those packages. Remove direct or transitive runtime dependencies that disappear after pruning: Gin, gRPC, SFTP, MinIO, Redis/cache/session stores, QUIC, OAuth, protobuf, Docker/release-only dependency support, and old CLI/config packages. If `.github/workflows/go.yml` still references removed integration suites, Docker setup, SFTP/MinIO setup, protobuf generation, `cmd/gofs`, or old package paths, reduce it to build/test the remaining library module with `go test ./...` and security/vulnerability steps that still apply.</action>
  <verify>
    <automated>go mod tidy && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` does not contain `github.com/gin-gonic/gin`.
    - `go.mod` does not contain `google.golang.org/grpc`.
    - `go.mod` does not contain `github.com/pkg/sftp`.
    - `go.mod` does not contain `github.com/minio/minio-go/v7`.
    - `.github/workflows/go.yml` does not reference removed paths `cmd/gofs`, `integration`, `sftp`, or `minio` unless those paths still exist after Plan 03.
  </acceptance_criteria>
  <done>Module files and CI test workflow reflect the reduced FTP library shape.</done>
</task>

<task type="auto">
  <name>Task 2: Run final dependency blacklist and full test proof</name>
  <files>ftpsync/dependency_boundary_test.go, go.mod, go.sum</files>
  <read_first>ftpsync/dependency_boundary_test.go, go.mod, go.sum, .planning/phases/08-ftp-only-package-reduction/08-CONTEXT.md</read_first>
  <action>Run the final proof commands and, if any blacklist item remains, remove the import/module source rather than weakening the blacklist. Commands: `go test ./... -count=1`, `go test ./ftpsync -run TestPackageDependencyBoundaryRejectsOldRuntime -count=1`, `go list -deps ./...`. Check `go.mod` and `go.sum` for forbidden dependency names from D-11. If a forbidden package appears only in `go.sum` after tidy and no package imports it, rerun `go mod tidy`; do not manually edit checksums unless Go tooling leaves an unreachable stale line that must be removed as plain text.</action>
  <verify>
    <automated>go test ./... -count=1 && go test ./ftpsync -run TestPackageDependencyBoundaryRejectsOldRuntime -count=1 && go list -deps ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0.
    - `go test ./ftpsync -run TestPackageDependencyBoundaryRejectsOldRuntime -count=1` exits 0.
    - `go list -deps ./...` output contains no blacklisted package/module names from D-11.
    - `go.mod` contains no Gin, gRPC, SFTP, MinIO, Redis/cache, QUIC, OAuth, or protobuf module requirements.
  </acceptance_criteria>
  <done>The reduced module has passing tests, a clean dependency blacklist, and tidied module files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go module resolver→source tree | `go mod tidy` updates dependencies based on imports in the reduced tree. |
| CI workflow→library verification | CI commands prove the package consumers build/test the reduced library path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | T | dependency blacklist | mitigate | Do not weaken forbidden list; fix import/module source when final proof fails. |
| T-08-04-02 | D | full test suite | mitigate | Run `go test ./... -count=1` after tidy to catch removed package references and stale CI assumptions. |
| T-08-04-03 | I | module files | accept | Module files list public package names only; no secrets are introduced. |
</threat_model>

<verification>
Run:
- `go mod tidy`
- `go test ./... -count=1`
- `go test ./ftpsync -run TestPackageDependencyBoundaryRejectsOldRuntime -count=1`
- `go list -deps ./...`
- `grep -E 'gin-gonic|google.golang.org/grpc|pkg/sftp|minio-go|nscache|quic-go|oauth2|protobuf' go.mod go.sum` returns no required runtime dependency entries.
</verification>

<success_criteria>
- All PRUNE-01 through PRUNE-04 requirements are covered by passing automated checks.
- `go.mod`/`go.sum` no longer retain old runtime dependencies.
- Phase 9 can document one clear reduced library package shape.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ftp-only-package-reduction/08-04-SUMMARY.md`.
</output>
