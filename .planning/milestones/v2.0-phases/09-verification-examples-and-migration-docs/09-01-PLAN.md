---
phase: 09-verification-examples-and-migration-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - ftpsync/validation_test.go
  - ftpsync/context_test.go
  - ftpsync/dependency_boundary_test.go
  - ftpsync/coverage_checklist_test.go
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-03
must_haves:
  truths:
    - "Developer tests and examples can import the package as import \"ftpsync/ftpsync\" while source remains in the ftpsync/ subdirectory under module ftpsync."
    - "Automated tests prove FTPSyncService construction, validation, cancellation, passive/default behavior, path encoding, and background shutdown coverage remains discoverable."
    - "Dependency boundary tests still reject old runtime and non-FTP dependency surfaces after the module path changes."
  artifacts:
    - path: "go.mod"
      provides: "Local module path selected by D-02"
      contains: "module ftpsync"
    - path: "ftpsync/coverage_checklist_test.go"
      provides: "Executable coverage checklist for VERIFY-01 and VERIFY-03"
      contains: "TestVerificationCoverageChecklist"
    - path: "ftpsync/dependency_boundary_test.go"
      provides: "Reduced package graph and stale import guard"
      contains: "go list -deps ./..."
  key_links:
    - from: "go.mod"
      to: "ftpsync/*_test.go"
      via: "package-external imports"
      pattern: "import \\\"ftpsync/ftpsync\\\""
    - from: "ftpsync/coverage_checklist_test.go"
      to: "ftpsync/*_test.go"
      via: "named coverage target strings"
      pattern: "TestVerificationCoverageChecklist"
---

<objective>
Change the module contract to `module ftpsync` with the current subdirectory package import path `ftpsync/ftpsync`, and add an executable coverage checklist for the final verification requirements.

Purpose: Phase 9 adoption depends on tests and examples compiling exactly the way local consumers will import the library while the package remains in the `ftpsync/` subdirectory.
Output: Updated module path, updated external-style test imports, strengthened dependency boundary guard, and a coverage checklist test.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-verification-examples-and-migration-docs/09-CONTEXT.md
@.planning/phases/09-verification-examples-and-migration-docs/09-RESEARCH.md
@go.mod
@ftpsync/service.go
@ftpsync/options.go
@ftpsync/validation_test.go
@ftpsync/context_test.go
@ftpsync/dependency_boundary_test.go

<interfaces>
Current public contracts that must remain importable from `import "ftpsync/ftpsync"` while package source stays in `ftpsync/`:

```go
type FTPSyncService struct { /* private fields */ }
func NewFTPSyncService(opts Options) (*FTPSyncService, error)
func (s *FTPSyncService) Validate() error
func (s *FTPSyncService) SyncOnce(ctx context.Context) (Result, error)
func (s *FTPSyncService) StartBackground(ctx context.Context) (Handle, error)

type Options struct {
    Source      Endpoint
    Destination Endpoint
    Direction   Direction
    Retry       RetryOptions
    IgnoreRules []IgnoreRule
    Hooks       HookSet
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch module and external tests to local ftpsync import path</name>
  <files>go.mod, go.sum, ftpsync/validation_test.go, ftpsync/context_test.go</files>
  <read_first>go.mod, ftpsync/validation_test.go, ftpsync/context_test.go, .planning/phases/09-verification-examples-and-migration-docs/09-CONTEXT.md</read_first>
  <action>Change the first line of `go.mod` from `module github.com/no-src/gofs` to `module ftpsync`. Because the package source remains in the `ftpsync/` subdirectory, update every package-external test import of `github.com/no-src/gofs/ftpsync` in `ftpsync/validation_test.go`, `ftpsync/context_test.go`, and any other package-external `ftpsync/*_test.go` file to `ftpsync/ftpsync`. Do not introduce `github.com/no-src/gofs/ftpsync` anywhere as the final consumer import path; historical mentions are allowed only in migration documentation created by a later plan. Run `go mod tidy` after edits so `go.sum` matches the module graph.</action>
  <verify>
    <automated>go test ./ftpsync -run 'TestValidateAcceptsSupportedDirections|TestContextAwarePublicContractsCompile' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` line 1 is exactly `module ftpsync`.
    - `ftpsync/validation_test.go` contains `"ftpsync/ftpsync"` and does not contain `github.com/no-src/gofs/ftpsync`.
    - `ftpsync/context_test.go` contains `"ftpsync/ftpsync"` and does not contain `github.com/no-src/gofs/ftpsync`.
    - `go test ./ftpsync -run 'TestValidateAcceptsSupportedDirections|TestContextAwarePublicContractsCompile' -count=1` exits 0.
  </acceptance_criteria>
  <done>The local module path and consumer-style test imports match the current subdirectory package layout and compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add executable final verification coverage checklist</name>
  <files>ftpsync/coverage_checklist_test.go</files>
  <read_first>ftpsync/validation_test.go, ftpsync/oneshot_test.go, ftpsync/background_test.go, ftpsync/context_test.go, ftpsync/internal_ftp.go, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: `TestVerificationCoverageChecklist` fails if expected verification target strings are removed from the test suite.
    - Test 2: checklist includes public service construction, validation, one-shot local→FTP, one-shot FTP→local, cwd safety, path encoding, passive mode, cancellation, background lifecycle, and background shutdown target names.
  </behavior>
  <action>Create `ftpsync/coverage_checklist_test.go` in package `ftpsync`. Implement `TestVerificationCoverageChecklist(t *testing.T)` that reads the relevant source/test files with `os.ReadFile` and asserts exact strings are present. Required mappings: `NewFTPSyncService` and `TestValidateAcceptsSupportedDirections` in `validation_test.go`; `TestSyncOnceLocalToFTP`, `TestSyncOnceFTPToLocalSuccess`, and `TestSyncOnceFTPToLocalNeverWritesToCWD` in `oneshot_test.go`; `newFTPPathCodec`, `ftpEncodingAuto`, `ftpEncodingUTF8`, and `ftpEncodingGBK` in `internal_ftp.go`; `PassiveMode` in `options.go`; `TestSyncOnceChecksValidationAndContext` and `TestStartBackgroundChecksValidationAndContext` in `context_test.go`; `TestBackgroundHandleWait`, `TestBackgroundStopShutsDownDeterministically`, and `TestBackgroundContextCancelStopsRunner` in `background_test.go`. Use table-driven subtests and `strings.Contains`; if a string is missing, fail with file path and expected string.</action>
  <verify>
    <automated>go test ./ftpsync -run TestVerificationCoverageChecklist -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `ftpsync/coverage_checklist_test.go` contains `func TestVerificationCoverageChecklist`.
    - The file contains exact strings `TestSyncOnceLocalToFTP`, `TestSyncOnceFTPToLocalSuccess`, `TestSyncOnceFTPToLocalNeverWritesToCWD`, `newFTPPathCodec`, `ftpEncodingAuto`, `TestBackgroundStopShutsDownDeterministically`, and `TestBackgroundContextCancelStopsRunner`.
    - `go test ./ftpsync -run TestVerificationCoverageChecklist -count=1` exits 0.
  </acceptance_criteria>
  <done>VERIFY-01 and VERIFY-03 have an executable checklist preventing accidental removal of required coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Update dependency boundary assertions for the local module</name>
  <files>ftpsync/dependency_boundary_test.go</files>
  <read_first>ftpsync/dependency_boundary_test.go, go.mod, .planning/phases/09-verification-examples-and-migration-docs/09-CONTEXT.md</read_first>
  <action>Update `ftpsync/dependency_boundary_test.go` so the dependency blacklist still rejects old runtime package fragments after the module path change. Keep the existing forbidden third-party fragments for Gin, gRPC, SFTP, MinIO, Redis/cache, QUIC, OAuth, and protobuf. Keep old `github.com/no-src/gofs/...` runtime fragments in the blacklist as stale import guards. Add an assertion that `go list ./...` run from repo root returns exactly one package path, `ftpsync/ftpsync`, after trimming whitespace. Keep the command string `go list -deps ./...` discoverable in the test.</action>
  <verify>
    <automated>go test ./ftpsync -run 'TestPackageDependencyBoundaryRejectsOldRuntime' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `ftpsync/dependency_boundary_test.go` contains `go list ./...`.
    - `ftpsync/dependency_boundary_test.go` contains `ftpsync/ftpsync`.
    - `ftpsync/dependency_boundary_test.go` still contains `github.com/no-src/gofs/cmd`, `github.com/gin-gonic/gin`, `google.golang.org/grpc`, and `github.com/minio/minio-go/v7`.
    - `go test ./ftpsync -run 'TestPackageDependencyBoundaryRejectsOldRuntime' -count=1` exits 0.
  </acceptance_criteria>
  <done>The reduced module graph remains guarded after the module path switch required by D-02 and D-15.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer import path → package API | External-style tests import the package as a local module consumer would. |
| Test process → filesystem | Coverage checklist reads repository source/test files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Spoofing | Module import path | mitigate | Assert `go.mod` is `module ftpsync` and external tests import `"ftpsync/ftpsync"`, not stale remote paths. |
| T-09-01-02 | Tampering | Dependency boundary | mitigate | Keep `go list -deps ./...` blacklist coverage for old runtime and non-FTP dependencies. |
| T-09-01-03 | Information Disclosure | Test failures with FTP options | mitigate | Preserve existing password-leak tests in `validation_test.go`; do not print option structs containing passwords in new tests. |
| T-09-01-04 | Denial of Service | Coverage checklist file reads | accept | Test reads a bounded set of local repository files only. |
</threat_model>

<verification>
Run `go test ./...` after all tasks in this plan. Confirm `go list ./...` prints only `ftpsync/ftpsync`.
</verification>

<success_criteria>
- `go.mod` uses `module ftpsync`.
- External-style tests compile with `import "ftpsync/ftpsync"` while the package remains in the `ftpsync/` subdirectory.
- Coverage checklist test exists and passes.
- Dependency boundary test passes and rejects stale runtime dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-examples-and-migration-docs/09-01-SUMMARY.md`.
</output>
