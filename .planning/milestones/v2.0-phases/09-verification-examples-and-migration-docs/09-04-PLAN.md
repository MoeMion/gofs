---
phase: 09-verification-examples-and-migration-docs
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - README.md
  - MIGRATION.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
must_haves:
  truths:
    - "README shows typed-option usage for one-shot push, one-shot pull, and background disk→FTP sync."
    - "README and migration docs identify `import \"ftpsync/ftpsync\"` as the supported local module package import path while source remains in ftpsync/."
    - "Migration docs clearly state removed and unsupported v2.0 surfaces."
  artifacts:
    - path: "README.md"
      provides: "Adoption guide and usage snippets"
      contains: "import \"ftpsync/ftpsync\""
    - path: "MIGRATION.md"
      provides: "Breaking migration notes"
      contains: "Removed and unsupported surfaces"
  key_links:
    - from: "README.md"
      to: "MIGRATION.md"
      via: "migration link"
      pattern: "MIGRATION\\.md"
---

<objective>
Replace stale application-oriented docs with final local-library adoption and migration documentation.

Purpose: D-10 through D-12 and DOC-01 through DOC-03 require developers to understand how to adopt the local `ftpsync` module and what old surfaces are no longer part of v2.0.
Output: README usage guide and discoverable migration notes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-verification-examples-and-migration-docs/09-CONTEXT.md
@README.md
@ftpsync/options.go
@ftpsync/service.go
@ftpsync/examples_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README for local ftpsync module adoption</name>
  <files>README.md</files>
  <read_first>README.md, ftpsync/options.go, ftpsync/service.go, ftpsync/examples_test.go, .planning/phases/09-verification-examples-and-migration-docs/09-CONTEXT.md</read_first>
  <action>Rewrite `README.md` so it describes the repository as the local Go module `ftpsync` with package source in the `ftpsync/` subdirectory, not the old gofs CLI/server application. Include these sections with exact headings: `# ftpsync`, `## Install as a local module`, `## One-shot local to FTP`, `## One-shot FTP to local`, `## Background local to FTP`, `## Supported package contract`, `## Limitations`, and `## Migration from old gofs runtime`. The install section must show a local embedding workflow using `replace ftpsync => ../path/to/ftpsync` or an equivalent local-module instruction, and every code snippet must import `"ftpsync/ftpsync"`. Usage snippets must show typed `ftpsync.Options`, `ftpsync.Endpoint`, `ftpsync.FTPOptions`, `ftpsync.DirectionLocalToFTP`, `ftpsync.DirectionFTPToLocal`, `PassiveMode: true`, `Timeout: 10 * time.Second`, and `PathEncoding: "utf8"`. Link to `MIGRATION.md` from the migration section. Remove old CLI install commands, Docker usage, file server, gRPC/HTTP server, task mode, SFTP, MinIO, YAML config, and URL-parser examples from the README because they are removed/unsupported v2.0 surfaces per D-11.</action>
  <verify>
    <automated>go test ./ftpsync -run 'ExampleFTPSyncService' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `# ftpsync`.
    - `README.md` contains `## One-shot local to FTP`, `## One-shot FTP to local`, and `## Background local to FTP`.
    - `README.md` contains `import "ftpsync/ftpsync"`, `DirectionLocalToFTP`, `DirectionFTPToLocal`, `PassiveMode: true`, `Timeout: 10 * time.Second`, and `PathEncoding: "utf8"`.
    - `README.md` contains `MIGRATION.md`.
    - `README.md` does not contain `go install github.com/no-src/gofs`, `docker pull nosrc/gofs`, `SFTP Push Client`, `MinIO Push Client`, `Task Server`, or `File Server`.
  </acceptance_criteria>
  <done>README presents only the supported local library contract and all required typed-option usage modes.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit breaking migration notes</name>
  <files>MIGRATION.md</files>
  <read_first>.planning/phases/09-verification-examples-and-migration-docs/09-CONTEXT.md, .planning/REQUIREMENTS.md, README.md</read_first>
  <action>Create `MIGRATION.md` with headings `# Migration to ftpsync v2.0`, `## Supported import path`, `## What changed`, `## Removed and unsupported surfaces`, `## Configuration migration`, and `## Capability mapping`. State that v2.0 is a breaking migration from the old CLI/server application to a local Go library module. State the supported import path exactly as `import "ftpsync/ftpsync"` because the package source remains in the `ftpsync/` subdirectory under module `ftpsync`. In `Removed and unsupported surfaces`, list these exact unsupported items from D-11: `no CLI runtime`, `no HTTP/gRPC/file server/task/auth/session runtime`, `no SFTP`, `no MinIO`, `no Docker release artifact`, `no FTPS`, `no FTP server mode`, `no FTP<->FTP sync`, `no FTP->disk background polling`, and `no bidirectional conflict resolution`. In `Configuration migration`, explain that callers now configure everything with typed Go `Options`, `Endpoint`, `FTPOptions`, `RetryOptions`, `IgnoreRule`, and `HookSet` instead of YAML or CLI config files. In `Capability mapping`, map old one-shot FTP push to `DirectionLocalToFTP` + `SyncOnce`, old one-shot FTP pull to `DirectionFTPToLocal` + `SyncOnce`, and old persistent local FTP push to `DirectionLocalToFTP` + `StartBackground`.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `MIGRATION.md` contains `# Migration to ftpsync v2.0`.
    - `MIGRATION.md` contains `import "ftpsync/ftpsync"`.
    - `MIGRATION.md` contains every exact unsupported-surface phrase listed in the action.
    - `MIGRATION.md` contains `Options`, `Endpoint`, `FTPOptions`, `RetryOptions`, `IgnoreRule`, and `HookSet`.
    - `MIGRATION.md` contains `DirectionLocalToFTP`, `DirectionFTPToLocal`, `SyncOnce`, and `StartBackground`.
    - `go test ./...` exits 0.
  </acceptance_criteria>
  <done>Migration notes satisfy D-10 through D-12 and DOC-02 through DOC-03.</done>
</task>

<task type="auto">
  <name>Task 3: Final doc consistency sweep</name>
  <files>README.md, MIGRATION.md</files>
  <read_first>README.md, MIGRATION.md, .planning/phases/09-verification-examples-and-migration-docs/09-CONTEXT.md</read_first>
  <action>Review `README.md` and `MIGRATION.md` for consistency with D-01 through D-12. Ensure final consumer docs do not present `github.com/no-src/gofs/ftpsync` as the supported import path. If the historical repository name appears, qualify it as the old runtime only and keep `import "ftpsync"` as the supported package path. Ensure deferred capabilities are clearly marked unsupported for v2.0 rather than implied available. Ensure README points to `MIGRATION.md` and migration notes point back to README for typed-option examples.</action>
  <verify>
    <automated>go test ./... && go test ./ftpsync -run 'ExampleFTPSyncService' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` and `MIGRATION.md` both contain `import "ftpsync/ftpsync"`.
    - `README.md` contains `MIGRATION.md` and `MIGRATION.md` contains `README.md`.
    - `README.md` does not contain `github.com/no-src/gofs/ftpsync`.
    - `go test ./... && go test ./ftpsync -run 'ExampleFTPSyncService' -count=1` exits 0.
  </acceptance_criteria>
  <done>Docs are internally consistent with the final local module contract and unsupported-surface boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → developer implementation | Developers may copy snippets and migration instructions into production projects. |
| Migration notes → unsupported capability decisions | Docs define what developers should not attempt with v2.0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Spoofing | Import path docs | mitigate | README and migration docs must use `import "ftpsync/ftpsync"` and not final-consumer remote import paths while source remains in `ftpsync/`. |
| T-09-04-02 | Tampering | Copied examples | mitigate | Keep snippets aligned with compiler-checked examples and typed public API names. |
| T-09-04-03 | Repudiation | Removed surfaces | mitigate | List every removed/unsupported surface exactly in migration notes. |
| T-09-04-04 | Information Disclosure | Example credentials | mitigate | Use fake example credentials only; do not instruct storing real secrets in docs. |
</threat_model>

<verification>
Run `go test ./...`, `go test ./ftpsync -run 'ExampleFTPSyncService' -count=1`, and text checks from task acceptance criteria.
</verification>

<success_criteria>
- README shows local module installation and typed-option usage for all supported modes.
- Migration docs are discoverable from README and explicitly list removed/unsupported surfaces.
- Docs consistently identify `import "ftpsync/ftpsync"` as the supported package path.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-examples-and-migration-docs/09-04-SUMMARY.md`.
</output>
