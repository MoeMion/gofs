---
phase: 04-ftp-verification-discoverability
plan: "02"
subsystem: testing
tags: [ftp, integration, ci, github-actions, verification]

# Dependency graph
requires:
  - phase: 04-01
    provides: repo-owned FTP bootstrap scripts plus real push/pull fixture pairs for the shared integration harness
provides:
  - Tagged FTP integration coverage now has explicit real-server push and pull subtests in the integration package
  - GitHub Actions now provisions FTP on Ubuntu and executes the tagged FTP integration suite in CI
  - FTP verification no longer depends on a developer remembering local-only steps before trust can be established
affects: [ftp, integration, ci, verification, roadmap]

# Tech tracking
tech-stack:
  added: []
  patterns: [backend-specific integration coverage should pair repo-owned service bootstrap with a dedicated tagged go test command in CI]

key-files:
  created: [.planning/phases/04-ftp-verification-discoverability/04-02-SUMMARY.md]
  modified: [integration/integration_ftp_test.go, .github/workflows/go.yml, .gitignore]

key-decisions:
  - "Kept the FTP integration entrypoint intentionally thin and only made subtest names explicitly call out real-server push and pull coverage so CI failures stay obvious."
  - "Wired FTP into the existing Ubuntu-only backend setup section in `.github/workflows/go.yml` instead of inventing a separate workflow, preserving the repository's established SFTP/MinIO verification shape."
  - "Ignored FTP bootstrap runtime artifacts in `.gitignore` so repeated local and CI-style verification does not leave untracked `.vendor`, pid, or log files behind."

patterns-established:
  - "Protocol verification becomes first-class only when the same tagged suite can be run both locally and in CI with repo-owned bootstrap steps."
  - "Generated backend bootstrap artifacts must be ignored if they are created by routine verification commands."

requirements-completed: [TEST-01, TEST-02, TEST-03]

# Metrics
duration: 8min
completed: 2026-04-24
---

# Phase 4 Plan 02: FTP CI Verification Summary

**Tagged real-server FTP push/pull integration coverage now runs through the standard harness and is enforced in GitHub Actions with Ubuntu FTP provisioning.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-24T16:56:00Z
- **Completed:** 2026-04-24T17:04:00Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Tightened the FTP tagged integration suite so subtest names explicitly identify real-server push and pull coverage in CI output.
- Added Ubuntu-only `Init FTP` and `Test Integration FTP` steps to `.github/workflows/go.yml`, matching the existing backend verification pattern used for MinIO and SFTP.
- Closed the verification loop for repeated FTP runs by ignoring FTP bootstrap runtime artifacts that would otherwise remain as untracked workspace noise.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add a tagged FTP integration suite that executes the real fixture pairs** - `c5b5bc4` (feat)
2. **Task 2: Wire FTP provisioning and tagged integration execution into CI** - `bd4904b` (chore)

**Plan metadata:** Pending

## Files Created/Modified
- `integration/integration_ftp_test.go` - Keeps the tagged FTP suite thin while making push and pull real-server failures explicit in subtest names.
- `.github/workflows/go.yml` - Adds Ubuntu-only FTP bootstrap and tagged FTP integration execution alongside the existing backend setup/test steps.
- `.gitignore` - Ignores FTP bootstrap runtime outputs such as `.vendor`, pid, and log files created during repeated verification.
- `.planning/phases/04-ftp-verification-discoverability/04-02-SUMMARY.md` - Records plan execution, verification evidence, and deviations.

## Decisions Made
- Kept the integration file aligned with the existing SFTP/MinIO thin table-driven pattern rather than introducing FTP-specific harness logic.
- Put FTP CI coverage in the existing Go workflow backend-test section so FTP is treated as a standard protocol verification path.
- Treated the reported `550 No such file or directory` blocker as part of the Phase 4 verification surface and re-ran the live suite before and after changes; in the current tree the tagged suite passed, so the remaining work was to make that passing path enforced and clean in CI.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Ignored FTP bootstrap runtime artifacts generated by routine verification**
- **Found during:** Task 2 (Wire FTP provisioning and tagged integration execution into CI)
- **Issue:** Re-running `scripts/ftp/init-ftp.sh` created `.vendor`, pid, and log outputs under `scripts/ftp/`, which left untracked files after verification and would make repeated execution noisy.
- **Fix:** Added `scripts/ftp/.vendor/`, `scripts/ftp/ftp-server.pid`, and `scripts/ftp/ftp-server.log` to `.gitignore`.
- **Files modified:** `.gitignore`
- **Verification:** `git status --short` no longer reported those FTP bootstrap artifacts as untracked after rerunning the FTP suite.
- **Committed in:** `bd4904b` (part of task commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** The deviation was small but necessary to keep the new verification path repeatable and workspace-clean. No scope creep beyond correctness of the planned CI/testing workflow.

## Issues Encountered
- The user-reported FTP `550 No such file or directory` blocker did not reproduce in the current rerun; instead, the remaining gap was that Phase 4 still lacked CI enforcement and generated-artifact hygiene for the FTP verification path.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 4 now has all three plan summaries on disk, so the phase can be treated as execution-complete once metadata updates are committed.
- FTP verification now has both a dedicated tagged entrypoint and CI enforcement, which gives the verifier a stable path to rerun.

## Self-Check: PASSED

- Found summary file: `.planning/phases/04-ftp-verification-discoverability/04-02-SUMMARY.md`
- Found commit: `c5b5bc4`
- Found commit: `bd4904b`
