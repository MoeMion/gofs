---
phase: 05-public-ftpsyncservice-api-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ftpsync/doc.go, ftpsync/options.go, ftpsync/service.go, ftpsync/options_test.go]
autonomous: true
requirements: [API-01, API-02]
must_haves:
  truths:
    - "Developer can import package ftpsync without importing cmd, conf, flag, server, daemon, api, monitor, report, SFTP, or MinIO runtime surfaces."
    - "Developer can construct FTPSyncService from typed Go options only."
    - "Developer can express local path, FTP connection, remote path, passive mode, timeout, path encoding, retry behavior, and ignore rules as Go values."
  artifacts:
    - path: "ftpsync/doc.go"
      provides: "Package declaration and public package documentation for ftpsync"
      contains: "package ftpsync"
    - path: "ftpsync/options.go"
      provides: "Typed public option structs and direction constants"
      exports: ["Endpoint", "FTPOptions", "Options", "Direction", "RetryOptions", "IgnoreRule"]
    - path: "ftpsync/service.go"
      provides: "FTPSyncService type and constructor"
      exports: ["FTPSyncService", "NewFTPSyncService"]
    - path: "ftpsync/options_test.go"
      provides: "Import, construction, and typed option coverage"
  key_links:
    - from: "ftpsync/service.go"
      to: "ftpsync/options.go"
      via: "NewFTPSyncService accepts Options and stores a sanitized service copy"
      pattern: "func NewFTPSyncService\(.*Options"
    - from: "ftpsync/options_test.go"
      to: "ftpsync"
      via: "external-style package test imports public API only"
      pattern: "NewFTPSyncService"
---

<objective>
Create the public `ftpsync` package entrypoint and typed option contract for `FTPSyncService`.

Purpose: Phase 5 starts by giving library consumers a focused Go API boundary that does not require the legacy CLI/config/server runtime.
Output: Importable package, public typed option structs, constructor, and tests proving typed construction covers API-01 and API-02.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/milestones/v1.0-phases/01-ftp-endpoint-contract-routing/01-01-SUMMARY.md
@.planning/milestones/v1.0-phases/02-ftp-driver-backend/02-01-SUMMARY.md

<interfaces>
Existing FTP endpoint contract from `core/vfs.go` that this public API must represent as typed Go values, without exposing `core.VFS` publicly:

```go
type FTPConfig struct {
	Username string
	Password string
	Timeout string
	Encoding string
	PassiveMode bool
}
```

Existing retry contract from `retry/retry.go`; public API must not expose this internal interface, but typed retry options must be able to build equivalent behavior later:

```go
type Retry interface {
	Do(f func() error, desc string) wait.Wait
	DoWithContext(ctx context.Context, f func() error, desc string) wait.Wait
	Count() int
	WaitTime() time.Duration
}
```

Existing ignore path contract from `ignore/path_ignore.go`; public API must expose typed ignore rules rather than file parser-only configuration:

```go
type PathIgnore interface {
	MatchPath(path, caller, desc string) bool
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the public typed option surface</name>
  <files>ftpsync/doc.go, ftpsync/options.go, ftpsync/options_test.go</files>
  <behavior>
    - Test 1: package `ftpsync` exposes typed options without importing `conf.Config`, `core.VFS`, URL strings, YAML config, or CLI flags.
    - Test 2: options can express local path, FTP host, port, username, password, remote path, passive mode, timeout, path encoding, retry count/wait/async, and ignore rules per API-02.
    - Test 3: zero-value `Options` is accepted by the compiler but remains subject to constructor validation in Task 2.
  </behavior>
  <action>Create package directory `ftpsync`. Add `doc.go` with package documentation and `package ftpsync`. Add `options.go` with exported Go structs/enums only: `Direction` constants for local→FTP and FTP→local, `Endpoint` for local/FTP endpoint descriptions, `FTPOptions` for Host, Port, Username, Password, RemotePath, PassiveMode, Timeout, PathEncoding, `RetryOptions` for Count, Wait, Async, `IgnoreRule` for literal/glob/regexp-style rule data, and `Options` combining Source, Destination, Direction, Retry, IgnoreRules, and hook fields reserved for Plan 03. Do not import `conf`, `core`, `flag`, `cmd`, YAML utilities, URL parsing helpers, or server/runtime packages; this implements API-02 through typed Go values only.</action>
  <verify>
    <automated>go test ./ftpsync -run TestOptions -count=1</automated>
  </verify>
  <done>`ftpsync/options.go` compiles, public option fields cover every API-02 configuration item, and tests fail if legacy config/parser types are required for option construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add FTPSyncService construction without legacy runtime imports</name>
  <files>ftpsync/service.go, ftpsync/options_test.go</files>
  <behavior>
    - Test 1: `ftpsync.NewFTPSyncService(options)` returns `*FTPSyncService` for a complete typed local→FTP option set.
    - Test 2: a test in external package style can import `github.com/no-src/gofs/ftpsync` and construct the service.
    - Test 3: package import checks fail if `ftpsync` imports old runtime packages: `cmd`, `conf`, `flag`, `server`, `daemon`, `api`, `monitor`, or `report`.
  </behavior>
  <action>Create exported `FTPSyncService` in `ftpsync/service.go` with constructor `NewFTPSyncService(opts Options) (*FTPSyncService, error)`. Store a private copy of `Options` and keep all fields library-local. The constructor must not start sync, open FTP connections, initialize global loggers, create reports, parse YAML, parse CLI flags, or call `cmd.Run*`; it only constructs and validates the public contract. Add tests that prove import and construction per API-01 and use `go list -deps ./ftpsync` in verification or test helpers to assert forbidden runtime packages are absent from the public package dependency graph.</action>
  <verify>
    <automated>go test ./ftpsync -run 'Test(NewFTPSyncService|PackageDependencyBoundary)' -count=1</automated>
  </verify>
  <done>Developers can import `ftpsync`, construct `FTPSyncService` from typed options, and dependency checks prove construction does not invoke or depend on legacy CLI/server/daemon/runtime surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→ftpsync.Options | Untrusted library caller values enter service construction. |
| ftpsync.Options→stored service state | Sensitive FTP credentials are copied into service-local memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | I | `FTPOptions.Password` | mitigate | Do not include password values in errors, package docs, or test failure messages; tests should assert error strings omit configured secret samples. |
| T-05-01-02 | T | `Options` construction | mitigate | Keep all public config typed; do not parse YAML/CLI/URL strings in the constructor, eliminating parser injection and ambiguous string grammar in this phase. |
| T-05-01-03 | E | package dependency boundary | mitigate | Add dependency-boundary test so importing `ftpsync` cannot transitively start or expose cmd/server/daemon/runtime capabilities. |
</threat_model>

<verification>
Run `go test ./ftpsync -count=1`. Also run `go list -deps ./ftpsync` and confirm no dependency path matches `/cmd`, `/conf`, `/flag`, `/server`, `/daemon`, `/api`, `/monitor`, `/report`, `/driver/sftp`, or `/driver/minio`.
</verification>

<success_criteria>
- API-01 is covered: `FTPSyncService` is constructible from an imported `ftpsync` package without old runtime imports.
- API-02 is covered: typed Go options cover all required endpoint, FTP, retry, and ignore configuration values.
- No legacy YAML/CLI parser or URL grammar is part of the public API.
</success_criteria>

<source_audit>
GOAL: Developers can construct and validate focused `ftpsync.FTPSyncService` using typed Go options — COVERED by Plans 01-03.
REQ API-01 — COVERED by Plan 01.
REQ API-02 — COVERED by Plan 01.
REQ API-03 — COVERED by Plan 02.
REQ API-04 — COVERED by Plan 02.
REQ API-05 — COVERED by Plan 03.
RESEARCH/HISTORY: Reuse existing FTP config semantics from v1 without exposing `core.VFS` or CLI parser — COVERED by Plan 01 and Plan 02.
CONTEXT decisions: `ftpsync`, `FTPSyncService`, typed options only, no YAML/CLI parser, no FTPS/FTP↔FTP/background FTP→disk — COVERED by Plans 01-03; deferred items excluded.
</source_audit>

<output>
After completion, create `.planning/phases/05-public-ftpsyncservice-api-contract/05-01-SUMMARY.md`
</output>
