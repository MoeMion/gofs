---
phase: 05-public-ftpsyncservice-api-contract
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [ftpsync/errors.go, ftpsync/service.go, ftpsync/validation_test.go, ftpsync/context_test.go]
autonomous: true
requirements: [API-03, API-04]
must_haves:
  truths:
    - "Developer receives explicit validation failures before transfer work starts."
    - "Developer cannot configure unsupported local↔local, FTP↔FTP, non-FTP, missing-path, or ambiguous endpoint combinations."
    - "Developer can pass context.Context to public sync methods and classify validation, cancellation, connection/authentication, transfer, and unsupported-capability errors."
  artifacts:
    - path: "ftpsync/errors.go"
      provides: "Public error kinds and classification helpers"
      exports: ["ErrorKind", "Error", "IsKind", "ErrValidation", "ErrCanceled", "ErrConnection", "ErrAuthentication", "ErrTransfer", "ErrUnsupportedCapability"]
    - path: "ftpsync/service.go"
      provides: "Constructor validation and public context-aware method contracts"
      exports: ["Validate", "SyncOnce", "StartBackground"]
    - path: "ftpsync/validation_test.go"
      provides: "Unsupported combination and missing/ambiguous field coverage"
    - path: "ftpsync/context_test.go"
      provides: "Context cancellation and error classification coverage"
  key_links:
    - from: "ftpsync/service.go"
      to: "ftpsync/errors.go"
      via: "validation and method contracts return typed *Error values"
      pattern: "ErrorKind|ErrValidation|ErrCanceled|ErrUnsupportedCapability"
    - from: "ftpsync/context_test.go"
      to: "ftpsync/service.go"
      via: "context cancellation is checked before transfer capability dispatch"
      pattern: "context\.Context"
---

<objective>
Add explicit validation and public context/error contracts to the `FTPSyncService` API.

Purpose: Developers need deterministic API feedback before any transfer starts, with errors they can classify in library code.
Output: Public error types/helpers, validation coverage for supported directions and forbidden combinations, and context-aware sync method signatures.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/05-public-ftpsyncservice-api-contract/05-01-SUMMARY.md
@.planning/milestones/v1.0-phases/03-one-way-ftp-sync-flows/03-01-SUMMARY.md

<interfaces>
Interfaces created by Plan 01 and consumed here:

```go
type FTPSyncService struct { /* private fields only */ }
func NewFTPSyncService(opts Options) (*FTPSyncService, error)

type Direction int
const (
	DirectionLocalToFTP Direction = ...
	DirectionFTPToLocal Direction = ...
)

type Options struct {
	Source Endpoint
	Destination Endpoint
	Direction Direction
	Retry RetryOptions
	IgnoreRules []IgnoreRule
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement public error taxonomy and classifiers</name>
  <files>ftpsync/errors.go, ftpsync/context_test.go</files>
  <behavior>
    - Test 1: validation failures can be detected with `ftpsync.IsKind(err, ftpsync.ErrValidation)`.
    - Test 2: canceled contexts can be detected with `errors.Is(err, context.Canceled)` and `ftpsync.IsKind(err, ftpsync.ErrCanceled)`.
    - Test 3: connection, authentication, transfer, and unsupported-capability kinds are exported and wrap underlying errors with `%w` semantics.
  </behavior>
  <action>Create `ftpsync/errors.go` with exported `ErrorKind` values for validation, cancellation, connection, authentication, transfer, and unsupported capability failures. Add exported `Error` struct implementing `Error()`, `Unwrap()`, and `Kind()`; add `IsKind(err error, kind ErrorKind) bool`. Ensure error messages include field names and capability names but never include FTP passwords. Use Go standard `errors.As`/`errors.Is` compatibility.</action>
  <verify>
    <automated>go test ./ftpsync -run 'Test(ErrorKind|ErrorWrapping|ContextCancellation)' -count=1</automated>
  </verify>
  <done>Public callers can classify all API-04 error categories without string matching, and wrapped causes remain available via standard errors helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate supported directions and reject unsupported endpoint combinations</name>
  <files>ftpsync/service.go, ftpsync/validation_test.go</files>
  <behavior>
    - Test 1: valid local→FTP options pass construction and `Validate()`.
    - Test 2: valid FTP→local options pass construction and `Validate()`.
    - Test 3: local↔local, FTP↔FTP, non-FTP, missing local path, missing FTP host, missing remote path, invalid port, and ambiguous Direction/source/destination combinations return validation or unsupported-capability errors before transfer work starts.
    - Test 4: validation errors do not contain FTP password values.
  </behavior>
  <action>Extend `NewFTPSyncService` and add `(*FTPSyncService).Validate() error` so Phase 5 rejects unsupported or ambiguous endpoint combinations per API-03. The only valid directions in this phase are local→FTP and FTP→local; FTP↔FTP, local↔local, unknown/non-FTP endpoints, missing local paths, missing FTP host, missing remote path, invalid port, and mismatched `Direction` versus source/destination roles must return typed errors. Do not add transfer implementation here; validation must complete before any later transfer work can be reached.</action>
  <verify>
    <automated>go test ./ftpsync -run TestValidate -count=1</automated>
  </verify>
  <done>`Validate()` deterministically accepts only local→FTP and FTP→local option sets and rejects every API-03 unsupported or ambiguous combination with typed, secret-safe errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add context-aware public sync method contracts</name>
  <files>ftpsync/service.go, ftpsync/context_test.go</files>
  <behavior>
    - Test 1: `SyncOnce(ctx)` checks validation and context cancellation before transfer dispatch.
    - Test 2: `StartBackground(ctx)` checks validation and context cancellation before lifecycle dispatch.
    - Test 3: FTP→local background startup returns unsupported capability because Phase 7 only allows disk→FTP background mode.
    - Test 4: public signatures accept `context.Context` and return structured `Result` / `Handle` contract values without importing legacy runtime result types.
  </behavior>
  <action>Add public methods `SyncOnce(ctx context.Context) (Result, error)` and `StartBackground(ctx context.Context) (Handle, error)` to `FTPSyncService`. Define minimal public `Result` and `Handle` contract types/interfaces needed for callers to compile against now; keep them library-local and independent of `result.Result` from the old runtime. Methods must call `Validate()` and map `ctx.Err()` to the cancellation error kind before any transfer/lifecycle dispatch. For capabilities scheduled for later phases, return `ErrUnsupportedCapability` with direction/method context rather than panicking or silently succeeding. Ensure FTP→disk background is rejected as unsupported per the milestone decision; disk→FTP background is allowed as an API shape but still returns unsupported capability until Phase 7 supplies implementation.</action>
  <verify>
    <automated>go test ./ftpsync -run 'Test(SyncOnce|StartBackground|Context)' -count=1</automated>
  </verify>
  <done>Public sync methods accept context, classify cancellation and unsupported capabilities, and prevent unsupported background FTP→disk or bidirectional behavior from entering the API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller options→validation | Untrusted caller configuration is normalized into supported/unsupported capability decisions. |
| caller context→method dispatch | Caller-controlled cancellation controls method lifecycle before transfer work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T | `Validate()` endpoint combinations | mitigate | Exhaustive table-driven validation rejects unsupported endpoint role combinations before any transfer or connection path is invoked. |
| T-05-02-02 | I | error messages | mitigate | Tests assert FTP password samples are absent from validation and method errors. |
| T-05-02-03 | D | context-aware methods | mitigate | Check `ctx.Err()` before dispatch and wrap cancellation with typed errors so callers can stop promptly. |
| T-05-02-04 | E | background API | mitigate | Explicitly reject FTP→disk polling and FTP↔FTP/bidirectional capabilities in public method contracts. |
</threat_model>

<verification>
Run `go test ./ftpsync -count=1` and `go test ./... -run TestNonExistent -count=1` to confirm the new package compiles within the module without requiring broader runtime startup.
</verification>

<success_criteria>
- API-03 is covered: supported and unsupported directions are validated explicitly before transfer work.
- API-04 is covered: public methods accept `context.Context` and expose structured error classification for validation, cancellation, connection/authentication, transfer, and unsupported capability categories.
- Deferred FTP→disk background polling, FTP↔FTP, FTPS, and bidirectional conflict resolution remain unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-ftpsyncservice-api-contract/05-02-SUMMARY.md`
</output>
