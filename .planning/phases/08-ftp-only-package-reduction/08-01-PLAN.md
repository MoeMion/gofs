---
phase: 08-ftp-only-package-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ftpsync/dependency_boundary_test.go
  - ftpsync/public_api_test.go
autonomous: true
requirements: [PRUNE-01, PRUNE-03, PRUNE-04]
must_haves:
  truths:
    - "A failing automated guard identifies old runtime packages in the default library dependency graph."
    - "Public API tests prove `ftpsync` exposes typed options only and no old config/server/task types."
  artifacts:
    - path: "ftpsync/dependency_boundary_test.go"
      provides: "Dependency blacklist test for Phase 8 pruning"
      contains: "TestPackageDependencyBoundaryRejectsOldRuntime"
    - path: "ftpsync/public_api_test.go"
      provides: "Public API leak regression test"
      contains: "TestPublicAPIStaysTypedOptionsOnly"
  key_links:
    - from: "ftpsync/dependency_boundary_test.go"
      to: "go list -deps ./ftpsync"
      via: "exec.Command"
      pattern: "go list -deps ./ftpsync"
---

<objective>
Create the automated package-reduction safety net before implementation work.

Purpose: Phase 8 has high deletion and import-rewrite blast radius. The first plan makes the required end state executable: old runtime imports and public API leaks must fail tests until later plans remove them.
Output: Dependency blacklist and public API leak tests that encode D-06, D-10, D-11, and D-13.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ftp-only-package-reduction/08-CONTEXT.md
@.planning/phases/08-ftp-only-package-reduction/08-RESEARCH.md
@ftpsync/options.go
@ftpsync/service.go
@ftpsync/oneshot.go
</context>

<interfaces>
From `ftpsync/options.go`:
```go
type Direction string
const (
    DirectionLocalToFTP Direction = "local_to_ftp"
    DirectionFTPToLocal Direction = "ftp_to_local"
)
type Endpoint struct { LocalPath string; FTP FTPOptions }
type FTPOptions struct { Host string; Port int; Username string; Password string; RemotePath string; PassiveMode bool; Timeout time.Duration; PathEncoding string }
type Options struct { Source Endpoint; Destination Endpoint; Direction Direction; Retry RetryOptions; IgnoreRules []IgnoreRule; Hooks HookSet }
```

From `ftpsync/service.go`:
```go
func NewFTPSyncService(opts Options) (*FTPSyncService, error)
func (s *FTPSyncService) SyncOnce(ctx context.Context) (Result, error)
func (s *FTPSyncService) StartBackground(ctx context.Context) (Handle, error)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dependency blacklist regression test</name>
  <files>ftpsync/dependency_boundary_test.go</files>
  <read_first>ftpsync/options_test.go, ftpsync/hooks_test.go, .planning/phases/08-ftp-only-package-reduction/08-CONTEXT.md</read_first>
  <behavior>
    - Test fails while `go list -deps ./ftpsync` includes old runtime packages such as `github.com/no-src/gofs/core`, `github.com/no-src/gofs/sync`, `github.com/no-src/gofs/logger`, `github.com/no-src/gofs/driver/sftp`, `github.com/no-src/gofs/driver/minio`, `github.com/no-src/gofs/server`, or `github.com/no-src/gofs/api`.
    - Test fails while deps include third-party runtime modules such as `github.com/gin-gonic/gin`, `google.golang.org/grpc`, `github.com/pkg/sftp`, `github.com/minio/minio-go/v7`, `github.com/no-src/nscache`, `github.com/quic-go/quic-go`, `golang.org/x/oauth2`, or `google.golang.org/protobuf`.
  </behavior>
  <action>Create `ftpsync/dependency_boundary_test.go` with `TestPackageDependencyBoundaryRejectsOldRuntime`. Use `exec.Command("go", "list", "-deps", "./ftpsync")`; split stdout into lines; fail with a table of matched forbidden fragments. The forbidden list must include the internal package fragments and third-party module names from D-11 and `08-RESEARCH.md`. Do not weaken existing Phase 5 dependency tests; this test is stricter and should drive Phase 8 extraction per D-01/D-02.</action>
  <verify>
    <automated>go test ./ftpsync -run TestPackageDependencyBoundaryRejectsOldRuntime -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `ftpsync/dependency_boundary_test.go` contains `TestPackageDependencyBoundaryRejectsOldRuntime`.
    - File contains exact string `go list -deps ./ftpsync`.
    - Forbidden list contains `github.com/no-src/gofs/core`, `github.com/no-src/gofs/sync`, `github.com/gin-gonic/gin`, `google.golang.org/grpc`, `github.com/pkg/sftp`, and `github.com/minio/minio-go/v7`.
  </acceptance_criteria>
  <done>The dependency guard exists and currently exposes the dependency leaks later plans must eliminate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public API leak regression test</name>
  <files>ftpsync/public_api_test.go</files>
  <read_first>ftpsync/options.go, ftpsync/service.go, ftpsync/errors.go, ftpsync/hooks.go, .planning/phases/08-ftp-only-package-reduction/08-CONTEXT.md</read_first>
  <behavior>
    - Test proves public construction still uses only `ftpsync.Options`, `Endpoint`, `FTPOptions`, `RetryOptions`, `IgnoreRule`, `HookSet`, `DirectionLocalToFTP`, and `DirectionFTPToLocal`.
    - Test fails if public API signatures require or expose `conf.Config`, `core.VFS`, CLI URL parsing, server/task types, or global logger/reporting types.
  </behavior>
  <action>Create `ftpsync/public_api_test.go` with `TestPublicAPIStaysTypedOptionsOnly`. Use `reflect.TypeOf` on `NewFTPSyncService`, `(*FTPSyncService).SyncOnce`, and `(*FTPSyncService).StartBackground` to assert constructor input is exactly `ftpsync.Options`, `SyncOnce` input is `context.Context`, `StartBackground` input is `context.Context`, `SyncOnce` returns `ftpsync.Result,error`, and `StartBackground` returns `ftpsync.Handle,error`. Add a string scan over exported type names in this package, or compile-time references, proving no public type name contains `Config`, `VFS`, `Server`, `Task`, or `Logger` except the approved small `Logger` hook interface. This implements D-06 and PRUNE-03.</action>
  <verify>
    <automated>go test ./ftpsync -run TestPublicAPIStaysTypedOptionsOnly -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `ftpsync/public_api_test.go` contains `TestPublicAPIStaysTypedOptionsOnly`.
    - Test contains `reflect.TypeOf(NewFTPSyncService)`.
    - Test contains strings `Config`, `VFS`, `Server`, and `Task` as rejected public leak markers.
  </acceptance_criteria>
  <done>Public API leak protection is executable without changing the approved typed-options API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process→go tool | Tests execute `go list`; output is local toolchain metadata, not user input. |
| public caller→ftpsync API | Reflection asserts old config/server/task types do not enter public contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | T | dependency blacklist test | mitigate | Match full import path fragments and report exact offending dependency lines so runtime imports cannot be hidden. |
| T-08-01-02 | I | test failure output | mitigate | Do not print FTP option values or passwords; tests inspect type/dependency names only. |
</threat_model>

<verification>
Run `go test ./ftpsync -run 'Test(PackageDependencyBoundaryRejectsOldRuntime|PublicAPIStaysTypedOptionsOnly)' -count=1`.
</verification>

<success_criteria>
- Dependency blacklist test exists and lists all D-11 forbidden package/module families.
- Public API test exists and proves typed-options-only contracts per D-06.
- No production behavior changes occur in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ftp-only-package-reduction/08-01-SUMMARY.md`.
</output>
