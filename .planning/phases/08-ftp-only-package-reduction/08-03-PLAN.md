---
phase: 08-ftp-only-package-reduction
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - cmd/
  - action/
  - api/
  - auth/
  - conf/
  - core/
  - daemon/
  - driver/
  - flag/
  - monitor/
  - report/
  - server/
  - sync/
  - Dockerfile
  - scripts/build-docker.sh
  - .github/workflows/docker.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [PRUNE-01, PRUNE-02, PRUNE-03]
must_haves:
  truths:
    - "Default module packages contain only the FTP sync library and retained internal helpers."
    - "Old CLI/server/protocol runtime directories are removed or no longer part of `go list ./...`."
    - "The retained library still passes all `ftpsync` tests after old runtime deletion."
  artifacts:
    - path: "ftpsync/"
      provides: "Surviving public FTP sync library package"
      contains: "package ftpsync"
    - path: "go.mod"
      provides: "Root module definition for reduced library"
      contains: "module github.com/no-src/gofs"
  key_links:
    - from: "go list ./..."
      to: "ftpsync/"
      via: "default package discovery"
      pattern: "github.com/no-src/gofs/ftpsync"
---

<objective>
Remove old application/runtime package trees after the library no longer depends on them.

Purpose: D-08 requires deletion/isolation of CLI entrypoints, config runtime, daemon, HTTP/gRPC APIs, task/auth/session, SFTP, MinIO, non-FTP sync/monitor paths, and Docker/release surfaces that only served the old application runtime.
Output: A reduced root module whose default packages are the FTP library and necessary internal helpers only.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ftp-only-package-reduction/08-CONTEXT.md
@.planning/phases/08-ftp-only-package-reduction/08-02-SUMMARY.md
@ftpsync/doc.go
@ftpsync/options.go
@ftpsync/service.go
@ftpsync/oneshot.go
@ftpsync/background.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old runtime package trees from the default module</name>
  <files>cmd/, action/, api/, auth/, conf/, core/, daemon/, driver/, flag/, monitor/, report/, server/, sync/</files>
  <read_first>.planning/phases/08-ftp-only-package-reduction/08-CONTEXT.md, .planning/phases/08-ftp-only-package-reduction/08-02-SUMMARY.md, ftpsync/dependency_boundary_test.go</read_first>
  <action>After confirming Plan 02 made `ftpsync` independent from old packages, remove old runtime directories that are not part of the focused FTP library: `cmd/`, `action/`, `api/`, `auth/`, `conf/`, `core/`, `daemon/`, `driver/`, `flag/`, `monitor/`, `report/`, `server/`, and `sync/`. This is deletion by design per D-03/D-08, not a build-tag isolation. Do not remove `ftpsync/`, `internal/` helpers still imported by `ftpsync`, `.planning/`, `README*`, or Go module files. If a listed directory contains a retained helper still imported by `ftpsync`, move that exact helper into `ftpsync/` or `internal/ftpsync/` first and update imports so the deleted old directory is not needed.</action>
  <verify>
    <automated>go list ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go list ./...` output does not contain `/cmd`, `/api`, `/server`, `/sync`, `/driver/sftp`, or `/driver/minio`.
    - Repository no longer has Go packages under `cmd/`, `api/`, `server/`, `sync/`, or `driver/` on the default build path.
    - `go test ./ftpsync -count=1` passes after deletion.
  </acceptance_criteria>
  <done>Old application/runtime package trees are absent from default module package discovery.</done>
</task>

<task type="auto">
  <name>Task 2: Remove old Docker and release surfaces</name>
  <files>Dockerfile, scripts/build-docker.sh, .github/workflows/docker.yml, .github/workflows/release.yml</files>
  <read_first>Dockerfile, scripts/build-docker.sh, .github/workflows/docker.yml, .github/workflows/release.yml, .planning/phases/08-ftp-only-package-reduction/08-CONTEXT.md</read_first>
  <action>Remove Docker/release-only surfaces that exist to build or publish the old CLI/server binary: `Dockerfile`, `scripts/build-docker.sh`, `.github/workflows/docker.yml`, and `.github/workflows/release.yml`. Keep generic Go test/security workflows only if they still run package tests for the reduced module. This implements D-08 and PRUNE-01. Do not add new docs/release wording here; Phase 9 owns final migration documentation.</action>
  <verify>
    <automated>go test ./ftpsync -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile` is absent or no longer builds `cmd/gofs`.
    - `.github/workflows/docker.yml` is absent.
    - `.github/workflows/release.yml` is absent.
    - `scripts/build-docker.sh` is absent.
  </acceptance_criteria>
  <done>Old CLI/server binary release surfaces no longer remain in the reduced library module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository deletion→module discovery | Removing package trees changes what `go list ./...` and consumers can import. |
| CI/release surface→public consumers | Removing Docker/release workflows prevents stale CLI/server artifacts from being published as the v2 library product. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | D | package deletion | mitigate | Run `go list ./...` and `go test ./ftpsync -count=1` immediately after deletion to catch missing retained helpers. |
| T-08-03-02 | T | stale CI/release files | mitigate | Delete Docker/release workflows that target removed `cmd/gofs` surfaces so old runtime artifacts cannot be published accidentally. |
| T-08-03-03 | R | removed public old packages | accept | v2.0 intentionally removes old CLI/server runtime surfaces per PROJECT.md and D-08; Phase 9 documents migration. |
</threat_model>

<verification>
Run:
- `go list ./...`
- `go test ./ftpsync -count=1`
- `go test ./... -count=1`
</verification>

<success_criteria>
- Old runtime directories do not appear in `go list ./...`.
- Docker/release surfaces for the removed CLI/server product are gone.
- The FTP library test suite still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ftp-only-package-reduction/08-03-SUMMARY.md`.
</output>
