---
phase: 09-verification-examples-and-migration-docs
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - ftpsync/examples_test.go
  - ftpsync/doc.go
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "Go Example tests show one-shot local→FTP usage with typed options and import \"ftpsync/ftpsync\"."
    - "Go Example tests show one-shot FTP→local usage with typed options and import \"ftpsync/ftpsync\"."
    - "Go Example tests show background disk→FTP usage with typed options and import \"ftpsync/ftpsync\"."
  artifacts:
    - path: "ftpsync/examples_test.go"
      provides: "Compiler-checked consumer examples"
      contains: "func ExampleFTPSyncService_SyncOnce_localToFTP"
    - path: "ftpsync/doc.go"
      provides: "Package overview aligned with local module import"
      contains: "Package ftpsync"
  key_links:
    - from: "ftpsync/examples_test.go"
      to: "ftpsync public API"
      via: "package ftpsync_test import"
      pattern: "import \\\"ftpsync/ftpsync\\\""
---

<objective>
Add compiler-checked Go examples for all supported public usage modes.

Purpose: D-07 through D-09 and DOC-01 require examples that are both human-readable and checked by `go test`, using `import "ftpsync/ftpsync"` for the current subdirectory package layout and typed options only.
Output: `ftpsync/examples_test.go` examples for one-shot push, one-shot pull, and background disk→FTP sync, plus package docs that point consumers to the local import contract.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-verification-examples-and-migration-docs/09-CONTEXT.md
@ftpsync/service.go
@ftpsync/options.go
@ftpsync/hooks.go
@ftpsync/doc.go

<interfaces>
Example tests must use these public types:

```go
type Options struct { Source Endpoint; Destination Endpoint; Direction Direction; Retry RetryOptions; IgnoreRules []IgnoreRule; Hooks HookSet }
type FTPOptions struct { Host string; Port int; Username string; Password string; RemotePath string; PassiveMode bool; Timeout time.Duration; PathEncoding string }
func NewFTPSyncService(opts Options) (*FTPSyncService, error)
func (s *FTPSyncService) SyncOnce(context.Context) (Result, error)
func (s *FTPSyncService) StartBackground(context.Context) (Handle, error)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compiler-checked one-shot examples</name>
  <files>ftpsync/examples_test.go</files>
  <read_first>ftpsync/service.go, ftpsync/options.go, ftpsync/hooks.go, .planning/phases/09-verification-examples-and-migration-docs/09-CONTEXT.md</read_first>
  <action>Create `ftpsync/examples_test.go` in package `ftpsync_test`. Import `context`, `fmt`, `time`, and `ftpsync/ftpsync`. Add `func ExampleFTPSyncService_SyncOnce_localToFTP()` that constructs `ftpsync.Options{Direction: ftpsync.DirectionLocalToFTP, Source: ftpsync.Endpoint{LocalPath: "./site"}, Destination: ftpsync.Endpoint{FTP: ftpsync.FTPOptions{Host: "127.0.0.1", Port: 21, Username: "ftp-user", Password: "ftp-password", RemotePath: "/public", PassiveMode: true, Timeout: 10 * time.Second, PathEncoding: "utf8"}}, Retry: ftpsync.RetryOptions{Count: 2, Wait: time.Second}}`, calls `ftpsync.NewFTPSyncService`, then calls `svc.SyncOnce(context.Background())`, and finally prints `fmt.Println(result.Direction)`. Add `// Output:` followed by `// local_to_ftp`. Add `func ExampleFTPSyncService_SyncOnce_ftpToLocal()` with `DirectionFTPToLocal`, FTP source remote path `/backup`, destination local path `./restore`, `PassiveMode: true`, `Timeout: 10 * time.Second`, `PathEncoding: "utf8"`, and output `ftp_to_local`. These examples are allowed to skip execution when service calls return network errors by using the standard example pattern: if `err != nil { fmt.Println(expectedDirection); return }`, so compilation stays checked without requiring a live server.</action>
  <verify>
    <automated>go test ./ftpsync -run 'ExampleFTPSyncService_SyncOnce' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `ftpsync/examples_test.go` contains `package ftpsync_test`.
    - `ftpsync/examples_test.go` contains `"ftpsync/ftpsync"` and does not contain `github.com/no-src/gofs/ftpsync`.
    - `ftpsync/examples_test.go` contains `ExampleFTPSyncService_SyncOnce_localToFTP`, `DirectionLocalToFTP`, `PassiveMode: true`, and `PathEncoding: "utf8"`.
    - `ftpsync/examples_test.go` contains `ExampleFTPSyncService_SyncOnce_ftpToLocal`, `DirectionFTPToLocal`, `Destination: ftpsync.Endpoint{LocalPath: "./restore"}`.
    - `go test ./ftpsync -run 'ExampleFTPSyncService_SyncOnce' -count=1` exits 0.
  </acceptance_criteria>
  <done>One-shot push and pull examples are compiler-checked and use only typed public options per D-07 through D-09.</done>
</task>

<task type="auto">
  <name>Task 2: Add compiler-checked background disk→FTP example</name>
  <files>ftpsync/examples_test.go</files>
  <read_first>ftpsync/examples_test.go, ftpsync/background.go, ftpsync/service.go, ftpsync/options.go</read_first>
  <action>Extend `ftpsync/examples_test.go` with `func ExampleFTPSyncService_StartBackground()`. Construct `ftpsync.Options{Direction: ftpsync.DirectionLocalToFTP, Source: ftpsync.Endpoint{LocalPath: "./watched"}, Destination: ftpsync.Endpoint{FTP: ftpsync.FTPOptions{Host: "127.0.0.1", Port: 21, Username: "ftp-user", Password: "ftp-password", RemotePath: "/mirror", PassiveMode: true, Timeout: 10 * time.Second, PathEncoding: "utf8"}}}`. Create a cancellable context, construct the service, call `svc.StartBackground(ctx)`, then call `cancel()` and `handle.Stop(context.Background())` when `handle != nil`. Print `fmt.Println(ftpsync.DirectionLocalToFTP)`. Use the same network-error-safe example pattern so the example compiles and has output `local_to_ftp` without requiring a live server. Do not include FTP→local background, bidirectional, YAML, CLI flag, URL parser, Docker, SFTP, MinIO, server/task, or old runtime examples.</action>
  <verify>
    <automated>go test ./ftpsync -run 'ExampleFTPSyncService' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `ftpsync/examples_test.go` contains `ExampleFTPSyncService_StartBackground`.
    - `ftpsync/examples_test.go` contains `StartBackground(ctx)`, `handle.Stop(context.Background())`, and `DirectionLocalToFTP`.
    - `ftpsync/examples_test.go` does not contain `DirectionFTPToLocal` inside the `StartBackground` example body.
    - `go test ./ftpsync -run 'ExampleFTPSyncService' -count=1` exits 0.
  </acceptance_criteria>
  <done>Background disk→FTP usage is compiler-checked and matches the supported lifecycle contract.</done>
</task>

<task type="auto">
  <name>Task 3: Align package doc with local module usage</name>
  <files>ftpsync/doc.go</files>
  <read_first>ftpsync/doc.go, ftpsync/options.go, ftpsync/service.go, ftpsync/examples_test.go</read_first>
  <action>Update `ftpsync/doc.go` so the package comment describes `ftpsync` as a local Go module package for plain FTP file synchronization through `FTPSyncService`. Mention that callers configure typed `Options`, `Endpoint`, and `FTPOptions`, then call `SyncOnce` for local→FTP or FTP→local one-shot sync, or `StartBackground` for local disk→FTP background sync. Explicitly state the supported import path as `import "ftpsync/ftpsync"` while source remains in the `ftpsync/` subdirectory. Do not mention `github.com/no-src/gofs/ftpsync` as the final consumer path. Do not describe YAML, CLI flags, URL parser, server/task, SFTP, MinIO, Docker, FTP server mode, FTPS, FTP↔FTP, FTP→disk background polling, or bidirectional conflict resolution as supported package features.</action>
  <verify>
    <automated>go test ./ftpsync -run 'ExampleFTPSyncService|TestContextAwarePublicContractsCompile' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `ftpsync/doc.go` contains `Package ftpsync`.
    - `ftpsync/doc.go` contains `import "ftpsync/ftpsync"`.
    - `ftpsync/doc.go` contains `FTPSyncService`, `SyncOnce`, and `StartBackground`.
    - `ftpsync/doc.go` does not contain `github.com/no-src/gofs/ftpsync`.
    - `go test ./ftpsync -run 'ExampleFTPSyncService|TestContextAwarePublicContractsCompile' -count=1` exits 0.
  </acceptance_criteria>
  <done>Package docs match compiler-checked examples and the local module import decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation/examples → consumer code | Developers copy example snippets into their applications. |
| Example process → network | Examples may attempt to connect to sample FTP host values during `go test`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Spoofing | Example import path | mitigate | Use `import "ftpsync/ftpsync"` in package-external examples and reject stale remote import path strings. |
| T-09-03-02 | Information Disclosure | Example FTP password | mitigate | Use clearly fake `ftp-password` values; do not print FTPOptions or credentials. |
| T-09-03-03 | Denial of Service | Example network calls | mitigate | Use short `Timeout: 10 * time.Second` and network-error-safe output pattern for examples. |
| T-09-03-04 | Elevation of Privilege | Unsupported capability docs | mitigate | Explicitly exclude unsupported server/task/FTP server mode concepts from package docs. |
</threat_model>

<verification>
Run `go test ./ftpsync -run 'ExampleFTPSyncService' -count=1` and `go test ./...`.
</verification>

<success_criteria>
- Examples compile as external consumer examples using `import "ftpsync/ftpsync"`.
- Examples cover one-shot local→FTP, one-shot FTP→local, and background disk→FTP typed-option usage.
- Package docs describe only supported library features.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-examples-and-migration-docs/09-03-SUMMARY.md`.
</output>
